# Generated by node scripts/argocd-self-management.mjs
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: spire-crds
  namespace: argocd
  annotations:
    argocd.argoproj.io/ignore-healthcheck: 'true'
    kubeclaw.io/health-mode: observed
spec:
  project: identity
  sources:
    - repoURL: https://spiffe.github.io/helm-charts-hardened/
      chart: spire-crds
      targetRevision: 0.6.0
      helm:
        releaseName: spire-crds
        skipTests: true
        valueFiles:
          - $values/gitops/platform/values/spire-crds.yaml
    - repoURL: https://github.com/datrab/kubeclaw.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: spire-server
  syncPolicy:
    syncOptions:
      - FailOnSharedResource=true
      - ServerSideApply=true
