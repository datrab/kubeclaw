# Generated by node scripts/argocd-self-management.mjs
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: postgresql
  namespace: argocd
  annotations:
    argocd.argoproj.io/ignore-healthcheck: 'true'
    kubeclaw.io/health-mode: observed
spec:
  project: data-services
  sources:
    - repoURL: registry-1.docker.io/bitnamicharts
      chart: postgresql
      targetRevision: 18.5.15
      helm:
        releaseName: postgresql
        valueFiles:
          - $values/gitops/platform/values/postgresql.yaml
    - repoURL: https://github.com/datrab/kubeclaw.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: kubeclaw
  syncPolicy:
    syncOptions:
      - FailOnSharedResource=true
