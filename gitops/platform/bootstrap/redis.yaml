# Generated by node scripts/argocd-self-management.mjs
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: redis
  namespace: argocd
  annotations:
    argocd.argoproj.io/ignore-healthcheck: 'true'
    kubeclaw.io/health-mode: observed
spec:
  project: data-services
  sources:
    - repoURL: registry-1.docker.io/bitnamicharts
      chart: redis
      targetRevision: 28.1.0
      helm:
        releaseName: redis
        valueFiles:
          - $values/gitops/platform/values/redis.yaml
    - repoURL: https://github.com/datrab/kubeclaw.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: kubeclaw
  syncPolicy:
    syncOptions:
      - FailOnSharedResource=true
