# Genuine original semantic producer counterexample

Status: **incomplete acceptance, no production edits, no finding closure**. Design approvede07c5c and independentbe25cf require actual fresh same-input en/cs production, not only reopening a bundle assembled with precomputed inner hashes.

Fresh integration baseline `ff55ab962d16dc0eba5ee6aafa2150abaa723fb5`, tree `dcd8b5875bf08a414cbd2c9ebfbec52331486e26`; all4090 remote entries verified against isolated standalone checkout base `3d95e424488032dbfe2c7585d4008408a887757d`. Mandatory current remote resume docs/register and original requirements read; baseline c387 rederived exactly47 IDs matching current frozen scope. All newly written paths are uniquely run11-semantic-producer-* tests/evidence. Other agents own production.

The native test runs the original registered Review stage through original PipelineRunner/AdapterRuntime. Original Core constructs lifecycle, lease and attempt, original repository adapter reads the same real two-commit disposable Git repository, original prepareReview/buildReviewSnapshot selects/hydrates context and generates simplification facts/candidates, original governor/report builders run, and actual ArtifactStore/EffectJournal persist their outputs. No artificial lifecycle or PluginContext, handcrafted simplification evidence, resolver policy digest, changed-manifest digest or selected-context digest is supplied. Input test evidence comes from actually executing the disposable source behavior and the original evidence encoder; gate coverage uses its original contract helper to trigger the unchanged production bundle persistence path.

The local HTTP endpoint uses the admitted no-finding response shape from the original live-function test. This is an original registered local contract fixture, **not successful external model/Gateway verification**, and not proof of every requested provider/regression gate. The original Core events and full stored refs/values are in raw evidence.

All six original executions (en/cs × gate/lean/audit) succeeded, made exactly one original HTTP dispatch each, stored actual bundle/report, and had real attempt.completed/run.succeeded events. The final cross-locale equality assertion intentionally failed:0/1 test pass, zero skips. Full native129KB raw is `run11-semantic-producer-original-native.txt`; exact extracted classification is `run11-semantic-producer-classification.json`.

The earliest observed inner mismatch is **policyDigest for every profile**, from the owning original policy resolver. Same Git revisions/changedManifestDigest, selected original source/context and selection digest are equal across locales. Gate evidence bytes/values are equal but bundle/report/governor identities differ. Lean/audit additionally have different original simplification-facts bytes/digests despite equal parsed fact values; generated candidate manifests differ in both bytes/digests and parsed values. This is an actual upstream owner issue, not solely outer bundle serialization. No opaque inner digest was fixed or substituted to force a pass.

Two earlier fixture errors are explicitly preserved: raw `first-fixture` has correct production rejection of incorrectly encoded input string evidence; `second-fixture` records real successful Core/report but the test incorrectly expected bundle persistence without coverage. Fixes only corrected fixture inputs to original accepted contracts and real FileJournal.entry access. They are not represented as semantic defects.

Current coverage is initial snapshot + actual generated facts/candidates + valid first-attempt governor + stored report/bundle. Slice/expanded snapshot, later certified governor history, invalid governor, Summary and crash-replay acceptance are not yet established by this new fixture. Existing snapshot-only Czech proof remains separate and is not duplicated as completion.

Next: run this same original producer oracle with explicit approved semantic mode on the author's frozen candidate; retain upstream policy/fact/manifest failures. Any needed inner-owner extension requires a bounded design and independent/root approval, preserving historical defaults and stored identities. Do not change the shared SDK codec globally, precompute inner hashes, weaken the gate or claim complete47.
