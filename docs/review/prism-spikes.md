# Prism Foundation Spikes — Abgrenzung vom Produktionsreview

Prüfstand `85ddfcbfc15e078780ea0434fc167e6f9a9b9488`. Dies ist eine Umfangs-/Nutzungszuordnung, kein vollständiger Defektreview der vier Prototypen.

`spikes/prism/README.md` bezeichnet die Pakete ausdrücklich als isolierte Beweise und verbietet Produktionsimports. Repositorysuche `rg -n 'spikes/prism' skills packaging scripts package.json` findet den Top-level-Befehl verify:prism:spikes und scripts/bootstrap-prism-tests.sh, keinen Import/Entrypoint im produktiven skills-/packaging-Baum. Die private-Paketmanifeste haben eigene Lockfiles und Verifybefehle; die vier Prototypen werden deshalb nicht als zusätzliche ausgelieferte Pipelinekomponenten gezählt. Das bedeutet weder obsolet noch zur Entfernung freigegeben: die Repositoryverification nutzt sie weiter.

| Prototyp | Tatsächlich gelesener Einstieg/Vertrag | Zuordnung und Aussagegrenze |
|---|---|---|
| puck-adapter | src/adapter.ts exportiert projectDocument/actionToOperation; src/domain.ts eigener siebenartiger Node-/Reducer-Vertrag mit revision/root statt produktivem DesignDocument | [prism.studio](components/prism.studio.md), [prism.domain](components/prism.domain.md). Der Prototype remappt beim Duplicate Child-IDs; das ist kein Gegenbeweis zum separaten produktiven Domain-Duplicatebefund. Vitestfälle gelten diesem eigenen Modell. |
| mobile-editor | src/main.tsx startet React/Puck mit vier UIarten; speichert Puck-Data in localStorage unter prism-mobile-spike, Vite-/Playwrightkonfiguration | [prism.studio](components/prism.studio.md). Browserprototyp für Touch-/Desktopinteraktion ohne Controlsession, PostgreSQLrevision oder produktiven Operationadapter. |
| preview-isolation | server.mjs startet zwei Loopback-HTTPserver4173/4174; feste test-session, prism-preview.v1-Messagehülle, allow-scripts-iframe und eigener CSP | [prism.studio](components/prism.studio.md), [prism.renderer](components/prism.renderer.md). Isolationstestoberfläche ist nicht produktives preview.ts; dortige Message-/CSPeigenschaften dürfen nicht aus dem Spike übernommen werden. |
| postgres-retrieval | verify-local.mjs mit PGlite/pgvector, verify.mjs mit pg.Client/disposable DB; eigenes schema.sql/prism_spike.search_corpus_v1 und synthetische16D-Kategorievektoren | [prism.corpus](components/prism.corpus.md), [prism.storage](components/prism.storage.md). Relevanz/Determinismus/Eligibility und Laufzeit dieses separaten SQLfixtures, kein Control-Pooltransaktions-, Produktionsprovider- oder echte Corpusdatenbenchmark. |

Gelesen: gemeinsamer README, alle vier package.json, genannte Entrypoints, Adapter-/Domainmodell und beide Retrievalrunner; für die Abgrenzung relevante Imports/State-/Listenstellen. Keine funktionalen Änderungen. Vorhandene Verifybefehle: puck-adapter TypeScript+Vitest, mobile-editor TypeScript+Playwright, preview-isolation Playwright, postgres-retrieval standardmäßig PGlite (verify:server braucht PRISM_SPIKE_DATABASE_URL). In diesem begrenzten Zusatz keine Spiketests ausgeführt und keine Browser-/DB-/Paketinstallation vorgenommen. Bestehende frühere Spikeberichte sind historische Evidence, kein neuer grüner Produktionsnachweis. Insbesondere Serverretrieval führt eigenes SchemaDDL aus und verlangt eine ausdrücklich disposable DB.

Dokumentationszuordnung: `docs/spikes/prism-foundation-spikes.md` und `spikes/prism/README.md`; produktive Gegenstücke separat unter docs/architecture/prism-studio-interaction-v1.md, prism-prototype-runtime-v1.md, prism-storage-model-v1.md und prism-retrieval-ranking-v1.md. Nächster Schritt nur bei konkreter Produktregression: Originalspiketest als historischen Vergleich ausführen, anschließend denselben invarianten Fall gegen tatsächlichen produktiven Einstieg prüfen. Keine pauschale E2E- oder Lieferfähigkeit aus vier Verifyprogrammen ableiten.
