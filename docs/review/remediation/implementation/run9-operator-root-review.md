# Root: bounded original operator request projection

Bounded source independently reviewed; root27/27 native/interacting tests pass,
zero skips or failures. PCR-OBS-002 remains incomplete; frozen47 is8 verified/39
incomplete. Only duplicate JSON human-approval request bytes are projected, never
original Core/wait history or delivery receipts, and no record-count credit.

Root read the complete five production-file changes and original authority,
replay, receipt/CAS, quota and lock paths plus both author and independent reports.
Sourcec02566a originally reviewed at09bfc5a is preserved by current SDK-reconciled
authorc477cca7d8610e9314cee6a9dddb052db68d5703, remote
1aeaf57815292e7f8c6a827e33e766c366222cea. Root checked all3917 non-tree entries
against that fresh remote checkpoint before import. All five product files remain
byte-identical after conflict-free integration atop Prism/SDK9c8dde882cdb1299902ed3a82d0b3d67d20c945d.
Current root source0c924fd preserves the shared Worker Core and role engines.

The new explicit local action validates original human-approval selected provider,
stage/input/config/effect/attempt, genuine wait creation, single-use authorized
signal, historical expiry/resolution and successful successor attempt under the
existing Core run fence, wait-store read fence and delivery CAS. The final sync
callback receives owned clones of actual locked records; async callbacks cannot
authorize a write. Old v1 delivery receipt consumers reconstruct the exact old
candidate digest including no-transport fallback and preserve accepted receipts
across stale complete/fail writers. No v2, Discord, uncertain, externalized or
foreign producer is silently upgraded. An independent honestly rehashed duplicate
signal-key counterexample was fixed before approval; its failing raw is retained.

## Root completed evidence

- run9-root-operator-native.txt:27/27, zero skips, including original Core human
  approval, actual signed loopback HTTP, trusted local issuer resume, original
  one-minute expiry without clock replacement, immutable caller scope, full
  negative authority matrix, original process fences and actual SIGKILL release,
  stale receipt preservation, actual quota admission, shared read/transition
  ownership, original dispatch projection and SDK/HMAC compatibility.
- Exact net release7358 bytes includes full record and manual receipt overhead.
  Existing two records remain two. Real23000-byte/5-record store blocks later
  original work before projection, then admits one original notification with
  one extra POST; unchanged record quota subsequently blocks more work. Small
  requests that cannot pay projection overhead reject atomically.
- run9-root-operator-gates.txt: canonical affected lint, complete Foundation/Nova
  typechecks and original operator suite (five deliveries plus boundary) exit0.
  The historical fixture stageId-only correction preserves original assertions;
  original a43 failure is retained, no schema gate weakened.

The HTTP fixture proves trusted-caller issuer authorization, not authenticated
external ingress. Positive retention does not authenticate coordinated rewrites
of all trusted local stores. Sibling provider histories show actual built-helper
EPIPE/init70, not successful provider execution. No original archive, evidence,
logs, signals or source is deleted and no network message is sent by the local
retirement action. Other retention producers/native gates remain open.

Next: final independent current-head interaction review and exact-parent ff-only
publication against freshly reread9c8. Preserve raw bytes and all remote WIP refs;
do not treat this partial metadata package as completed OBS or all47.
