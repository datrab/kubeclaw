# Independent report artifact cutover acceptance

Decision: APPROVED for the bounded report-only producer/compiler/recovery cutover. PCR-SDK-001 and the frozen 47 are NOT closed. No production source change was authored by this reviewer.

## Exact source and recovery

Reviewed original design95880cc1 and independent design13a56494. Author production freeze ad242032, final source/tests4f81f0b6 and the single owning npm cwd correction d96b8744 are preserved byte-for-byte. Frozen remote2ae7f59cdc6d1a976f2de9e50f06139982abed7c provides the exact20 author delta paths; all20 Git blob identities were independently compared against the tested checkout.

Independent final execution used a NEW standalone private shallow checkout, not the vanished historical linked worktree: original root07d35b6978611ac0a4095cee8ecc3196419870c8 equals freshly read MAIN21b05129691b6c932fdf7fd2d12f906cb2736aad/tree268ca86b318b965ddb285cee6e093d4d98068ec6. Exact20 report paths were reconstructed from remote bodies, with no overlap against current cache/SDK/Prism/operator changes. Only the reviewer's own dependency cache moved; current workspace links resolve this checkout. One stale dangling link to the absent historical test-agent package was removed, not replaced with a provider shim.

The missing old Git metadata interruption lost no source or proof: additive native tests were first saved at fc5528409aae12b26eae5abb1597566ac5e4961b; current-head source and failure evidence at c7fd0c69c85099279b389396d9f2f43eb60d06ac. Current acceptance depends on current source and verified archived bytes, never old Git objects.

## Source review

The optional closed reportArtifactEncoding selector is validated by the owning stage parser before getter-prone config access or effects. Absence preserves legacy. Normal finalization, dispatch failure and semantic failure all carry the same selected mode into the single original report writer. New report bytes and returned complete ID/namespace/media type/producer/digest/size/tag are bound before acceptance. Unknown or conflicting modes cannot cause an extra write or synthetic receipt.

Both module and cumulative Review stages in genuinely new compiler output explicitly opt in. Source identity and report mode remain independent during recovery. Expected generated Review IDs/types come from a validated supplied-project skeleton; invalid/mixed/missing/extra nodes reject, followed by unchanged WHOLE-graph identity verification. Cumulative helper's omitted mode is deliberately legacy for the genuine archived compiler's current dependency. Existing package pins are unchanged.

Independent native authored-domain test confirmed module.review and final.review still accept only their original agent field: authored portable/legacy/unknown/null/present-undefined/getter selectors reject BEFORE compiler selection in both compiler modes, with zero getter reads. Valid agent-only project configs generate exact selected mode. No unsupported authoring fields were admitted to make a test pass.

Report.v2 semantic fields/item IDs, policy/bundle digests, logical report artifact ID and attachReviewReport facts retain their meanings. Conditional preceding storeReviewBundle remains exact legacy, with unchanged invocation position. No cache-profile widening.

## Executed acceptance evidence

- Complete original Review npm suite including its original pretest/test/schema checks, all14 new report tests and original registered portable live fixture passed exit0. Complete untruncated raw is run14-report-independent-quiet.txt. Original million-line bound stayed45,000ms; independent quiet result32,021ms.
- All16 native en-US/da-DK/tr-TR/sv-SE producer/replayer locale pairs preserve exact portable report ArtifactRef and completed journal bytes. Actual original builder, strict schema, ArtifactStore, EffectCoordinator and FileEffectJournal execute.
- Archived original untagged writer preserves same-locale request/reference replay. Known historical cross-locale integrity rejection remains fail-closed. Exact requested/accepted journal-prefix vectors preserve original bytes; these vectors are NOT described as an actual crash.
- Separate independent ACTUAL SIGKILL cases2/2 cover legacy and portable original writers. The original RecordStore fence holds the actual metadata write after original requested/accepted events; process SIGKILL and original restart preserve uncertainty, exact journal bytes and zero artifact metadata/blob writes. No journal synthesis/truncation, fake store or adapter is used.
- Original governor and project-summary actually consume strict original-builder portable reports across locales. Four independent missing/corrupt actual legacy/portable report blob cases pass; each checks the exact persisted adapter.effect_failed cause (ARTIFACT_NOT_FOUND or ARTIFACT_INTEGRITY_FAILED), original completed write prefix and no duplicate artifacts.write.
- Six supplementary independent/old-source compiler recovery cases passed. Genuine current-checkout archived CLI reproduces original unfixed graph mismatch, then original corrected CLI recognizes the same terminal blocked run without graph/journal mutation. This is terminal compiler/recovery compatibility, not pending report execution or native provider success.
- Original phase6 Core package pin/upgrade/cancellation suite passed STRICTLY AFTER the full Review run, retaining the original500ms cancellation bound. It proves owning Core checks, not authorization to upgrade an actual old Review package.
- Original Shared-runtime, Nova and Review typechecks plus all affected canonical configured lint passed in a separate fail-fast chain. Supplement/check raw files preserve these results.

## Failures preserved, not weakened

Initial new npm posttest used the Review package cwd for repository-relative original compiler fixtures. Independent execution exposed three failures and a stuck test process; author corrected only the new command's cwd, with no fixture/schema/assertion weakening. The interrupted full invocation was NOT counted as passed.

An initial concurrent-load run failed the unchanged million-line bound at52.346s and phase6's original cancellation bound. Full original suites were rerun sequentially in a coordinated quiet slot, with both exact assertions passing. Before-failure raw remains run14-report-independent-before.txt; the initial invalid Foundation typecheck path is likewise explicit, and the correct owning shared-runtime typecheck was rerun. Aggregate shell exit from that initial mixed command is not used as acceptance.

## Scope limits and handoff

Typed finding/governor and other summary gate evidence are explicit artifact-contract data. The registered Review/Git/HTTP fixture is not native model/gateway, browser, database, deployment or provider-quality proof. The report-only change does not port separate bundle/coverage identities or fix the governor reader's preexisting lack of full returned-reference comparison. It does not guess historical locale bytes, alter trusted package pins or migrate existing graphs.

Root may integrate only the exact independently reviewed report delta and these additive evidence/tests against its freshly reread branch head, preserving all current cache/SDK/Prism/operator source and author evidence. Changed source or dependency head requires affected rechecks. No CI, deployment, production changes, paid resources or third-party messages occurred.
