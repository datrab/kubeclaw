# Nova dispatch causal ownership correction — incomplete

This corrects the independent cross-Core counterexample preserved in
`run5-dispatch-independent-ownership-before.txt`. Manual operator policy does not
invent consumer ownership. The original Core snapshot must pin the real remote
adapter and artifact store. Original `readRunEvidence` validates the complete run;
the selected original request, accepted/completed receipts, Core attempt identity
and ordered effect audits must bind the exact job, stage, source and full plan.
The original quality stage's retained decision artifact must match both its
original artifact-write receipt and its completed Core attempt, with bounded,
digest-checked bytes. Old snapshots without this authority fail closed.

The revised positive fixture really executes the original quality stage, remote
adapter, Buster HTTP/service, result importer, artifact store and Core. Here the
native sandbox helper is not built: Buster produces an actual non-null errored
result, Nova imports the exact execution_error decision, and the original Core
blocks. An authenticated original administrative cancellation in disposable test
state then produces the actual terminal consumer checkpoint. This is confirmed
failed-history retention, **not a native provider pass**. No events or receipts
are fabricated. The independently authored foreign-Core assertion now rejects.

Four end-to-end cases pass without skips: exact replay/no redelivery and unchanged
quotas/blob/import bytes; legacy-only repair versus projected missing/corrupt blob
failure; stale scope/alias/run-owner refusal; independent cross-Core refusal.
The new causal helper, operator, extracted original dispatch store and fixture
pass unchanged canonical ESLint. Importer canonical lint still reports pre-existing
large validation/record functions plus the new retained-import callback and file
growth; correction and full regression remain required before approval.

Incomplete: independent causal review, wrong-stage/source/attempt controls,
complete evidence-reference negatives, three-fence end-to-end race/crash matrix,
bounded record-limit regression, canonical importer factoring, and reconciliation
against the latest repair head. Existing primitive two-process/SIGKILL evidence is
not a substitute for the operator matrix. No finding or package is closed.
