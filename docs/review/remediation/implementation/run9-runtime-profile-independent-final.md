# Final independent review: runtime dispatch selector no-trap correction

APPROVED for this bounded correction on fresh MAIN21b05129691b6c932fdf7fd2d12f906cb2736aad.
This supersedes the pending checkpoint; it does not close PCR-SDK-001 or all47.

## Exact ownership and source

Reviewer authored the pre-fix genuine HTTP oracle890797782ad99becd03532baacb2e87a462ef992,
not the correction. Author f505eedacada7e7168740aa27272530f62c9f47f moves the existing
portableJson(request) precondition before Object.hasOwn. Final author source c9a19ebb
and independently assembled candidate0a4bf95 contain the identical helper Git blob
c3839b6204e177a030b6680624ddf77d50270219. No other production file differs from the
freshly verified4024-entry main tree268ca86b318b965ddb285cee6e093d4d98068ec6.

The new standalone review checkout resolves all62 locked workspace links into
itself. Expired inherited alternates caused an unsuccessful shallow fetch; the
replacement snapshot preserves all4024 tracked entries including ignored raw
logs, without requiring historical Git objects. Remote mandatory resume records,
register and original finding text were read again; original baseline47 matches
the immutable scope exactly.

Original Core invocation and registered AdapterRuntime call the helper before
request field access. The full accepted JSON record remains byte-identical for
legacy requests and real internal EffectInvocation metadata: nested parent
EffectRequest, confidential boolean and optional delivery/dependency ownership.
Signals, callbacks and adapter implementations are separate parameters. No
serializer/codec/schema/model-control/public-confidential contract was changed.
Historical model keys remain ordinary model data. Core/role-engine architecture
is unchanged.

## Independently completed gates

- Original pre-fix actual context/registered AdapterRuntime HTTP oracle remains
  unchanged: before, both paths execute one trap and one real HTTP POST; after,
  both reject CANONICAL_JSON_PROXY_UNSUPPORTED with zero traps, zero POSTs and
  no admitted effect. Additive tests cover absent/present Proxy, accessors and
  nested invalid JSON without executing any getter/trap. Valid internal metadata
  and both legacy codec bytes are preserved.
- Earlier exact f505 review passed58 targeted cases, full original Review/SDK,
  historical CLI, owning types and generation before the cache/reader integration.
- On freshly integrated cache/reader MAIN21b051, original combined101 cases had
  100pass/1fail/0skip. The sole failure was real ArtifactStore ready startup
  exceeding the unchanged100ms fixture bound during v1 cache read/completed
  resume. It was not an expected SIGKILL, skip or codec/replay assertion.
  Full first raw remains in run9-runtime-profile-independent-final-101-first.txt
  and remote b9dea0da020d0c40f6c3cf5bf40c5fe843e98bdb.
- After coordinating an otherwise idle test slot, the ENTIRE unchanged24-case
  historical cache phase file passed24/24, zero skips,68.36s. No timeout,
  assertion, source, environment codec or test data was changed. All v1/v2/v3/v4
  requested/accepted/completed read/write prefixes and legacy wrong-locale denial
  passed. The initial startup timeout remains a real failed run; contention is
  a plausible context, not an independently established scheduler root cause.
- The fresh combined matrix otherwise passed every original transport producer,
  v4/cache profile, actual StageExecutor checkpoint/restart, corrupt/mixed cache
  owner, real HMAC/confidential/model-key, dependency corruption/uncertainty/
  concurrent-locale, genuine HTTP/SIGKILL and ArtifactStore/EffectJournal case.
- Complete original Prism fixture independently passed16 registered stage cases,
  24 historical replay phases,32 locale consumers and8 actual wrong-owner cases.
  All five original capability argument sequences/order remain unchanged; the
  independent reader Proxy probe and original Prism live/wait/archive tests pass.
- Fresh original historical CLI passes with the authentic old graph-mismatch
  negative and current terminal refusal preserving the13-stage graph. Full
  original Review passes, including unchanged million-line compiler36.2s,
  genuine Core/Git/HTTP/ArtifactStore, schema and coverage checks. SDK package
  tests, Foundation/Nova noEmit typechecks and generator --check exit0.

The targeted101 cases are therefore covered by the successful100 cases and full
unchanged24-case rerun, rather than falsely reporting the first process exit0.
No production fixes were introduced by the reviewer.

## Evidence and limits

Raw final files: run9-runtime-profile-independent-final-101-first.txt,
run9-runtime-profile-independent-final-24-quiet.txt,
run9-runtime-profile-independent-final-original.txt and
run9-runtime-profile-independent-final-types.txt under docs/review/evidence.
Earlier unchanged oracle/test and58-case raw remain in the preceding review
checkpoint eb209b91baab069c8c42a1eb0d68c7bc8d4ddd80.

The loopback503 receiver is NOT a native Gateway/Prism/model success. Cache seeded
contextRequest is explicitly a fixture, not a provider success. Uncertain old
effects remain unresolved; no guessed legacy locale or automatic migration was
introduced. Separate report/digest consumers and other original findings remain
open. Root must integrate only this approved source against freshly reread head
with exact-parent fast-forward, preserving existing changes and repeating any
newly affected interaction. This reviewer performs no MAIN update or CI/deploy.
