# Buster fixture lifecycle building blocks — not yet activated

The existing Buster runner is not replaced by this checkpoint. No finding closes;
137/154 remain locally verified and 17 incomplete. No live run or deployment.

- Separate durable fixture admission, immutable nonterminal readiness/scope
  binding, first teardown intent and final receipt from the original Core journal.
  Retained file bytes and pending readiness/result reservations count against disk
  limits. Corruption does not authorize replacement or erase previous identity.
- Optional bounded fourth process pipe for role-owned phase messages. Standard
  input/output retain their existing meaning. Channel failure stops the host;
  unfinished phase work fences admission through the original native executor.
  Ordinary Prism attempts do not request this pipe.
- Pool readiness now requires domain cgroups, actual peak counters and writable
  cgroup.kill before admission, instead of discovering missing support on launch.

Five actual file/journal cases and six actual child-process/pipe cases pass,
including writer SIGKILL and truncated/oversized frames. Readiness test values
are explicitly protocol vectors, not invented kernel evidence. The terminal
store case uses an original genuinely never-launched Core receipt. No positive
fixture host, cgroup execution or complete Buster pipeline is claimed.

Raw evidence: docs/review/evidence/pr6-buster-fixture/. Remaining: connect the
fixture host and dependency scheduler, retain one physical scope through teardown,
complete startup/chart/recovery and remove the superseded executable runner path.
Historical receipt reading is separate from an executable legacy fallback.
