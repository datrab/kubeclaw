# Independent inner-owner checkpoint — rejected incomplete slice

Exact source: remote 8ae8a0859e6111683fb87e810039488f3f9b0d52, author
d8f77ff08e351cdada12835015a593db6d25e59b, tree cfb9121afab4c62007e37d3b8fc5bb1d09d3cbf3.
New isolated reviewer checkout run15-semantic-inner-review was reconstructed as
a standalone snapshot and its complete tree matches all 4,107 remote entries.
No old local Git history is needed. Current remote mandatory resume documents,
register and original PCR-SDK-001 requirements were read; original c387 register
again yields exactly 47 IDs matching the frozen current scope. Approved designs
bbb072 and independent8ee1 were read in full before review.

Dependencies use the existing immutable third-party cache, with all62 current
workspace symlinks relative inside this checkout. Initial link audit identified
one additional dangling historical plugin-test-agent alias; only that own copied
symlink was removed. No linked third-party file, source or other checkout changed.

## Direct independent result: two pass, nine fail

Command: LANG=en_US.UTF-8 LC_ALL=en_US.UTF-8 node --test
tests/verification/reliability/review-inner-owner-independent.test.mjs

Complete raw: docs/review/evidence/run15-inner-owner-independent-before.txt.
Exit one; 11 cases, two pass, nine fail, zero skips. Original policy values for
gate/lean/audit, actual resolver precedence/authorized override, all provenance
digests, authentic private mode and clone rejection pass. Original generated
forwarding-source facts and original miner/certificate positive pass; old/new
logical facts and candidate IDs are equal and new source.digest references the
actual generated content. These are DIRECT OWNER checks, not a registered Core
lifecycle, real stored ArtifactStore reader, model or full native producer proof.

Eight direct no-trap negatives fail on the new owner boundaries:

- Fact context Proxy executes8 traps; content getter executes1.
- Fact revision Proxy executes10 traps; head getter executes1.
- Manifest input Proxy executes12 traps; policy getter executes7.
- Miner input Proxy executes7 traps; policy getter executes4.

The approved no-trap JSON admission must precede any such reflection on owning
plain JSON inputs. Do not blanket serialize method-bearing PluginContext values.
The ninth failure substitutes only the digest of genuinely generated valid facts:
the miner still emits a candidate referring to that incorrect source digest.
Its new parseSource verifies codec bytes but does not verify the content hash.
This is a direct miner authority failure, not a proven native stage bypass;
the full bundle reader has separate evidence validation. The approved design
requires wrong digest/content rejection at the actual owner boundary.

Root and author received the exact failures before any production correction.
The reviewer has made no production edits.

## Independently reproduced original legacy regression

Command: node skills/nova/plugins/review/tests/review-proposal-preflight.unit.test.mjs

The unchanged original fixture passes its original parsed Echo input and genuine
legacy resolved policy with a legal opaque placeholder bundle.policyDigest. The
new assertReviewPolicyBundle rejects at the original line38 call with
REVIEW_BUNDLE_POLICY_DIGEST_MISMATCH; exit one preserved in
docs/review/evidence/run15-inner-original-preflight-before.txt.

Restore this original exported legacy helper admission without weakening any
existing native validation. Explicit new bundle.v2 must still bind exact policy
digest and authentic resolver mode, and forged/cross-mode certificates reject.
Root conditionally authorized that narrowly bounded correction; no original
fixture/assertion is changed by this reviewer.

## Next action / acceptance limits

Await exact frozen author correction; independently rerun the unchanged permanent
oracle and unchanged preflight test, then check v2 policy mismatch, selected mode,
private reduction certificates and actual stored candidate-reader admission with
bounded direct checks. Preserve current remote metadata on every backup. No MAIN
write or finding closure. No heavy native matrix and no safety-flagged helper
operation retry, rephrasing or reroute. The native original producer proof remains
red only; required initial/slice/expanded/history/crash/storage acceptance and the
separate delivery-manifest owner are still incomplete.

## Corrected first oracle and original helper regression

Exact author correction 6cb67a0372ff516377c91124c6d1c8d0310b113f was applied to
own source e8f439a after the narrowly restored legacy guard. Production diff
against that frozen author source is empty. Unchanged original eleven-case oracle
passes 11/11, zero skips; unchanged original preflight suite passes. Complete raw
is run15-inner-owner-independent-after.txt. Author remote checkpoint51b37e66aa93a91b237fa7e5939a38a4ec540aef
preserves this same frozen production; no overall semantic package approval.

Setup chronology: cherry-picking the legacy correction before its author's test
creation commit caused an own-checkout modify/delete conflict. An incorrectly
sequenced diagnostic test then repeated the existing two-pass/nine-fail result
before6cb was applied. This is NOT a corrected-source failure. Full repeat raw is
run15-inner-correction-setup-repeat.txt. The own in-progress cherry-pick was aborted,
the prerequisite test commit applied, then the exact legacy and6cb corrections.
No author's checkout or production source was edited and no original assertion
was changed to fix setup.

## Next direct owner checks: thirteen pass, four fail

The expanded permanent oracle retains all original11 assertions. Explicit new
bundle policy mismatch and private reduction certificates pass, including old
opaque v1 helper compatibility. The actual candidate-reader FUNCTIONS also pass
decoded-byte checks without an in-process producer certificate: matching generated
manifest, corrupt digest, noncanonical bytes, wrong/missing codec and unknown
logical schema. This uses JSON roundtripping, NOT disk storage or a Core lifecycle.
It must not be called a native stored ArtifactStore acceptance gate.

Four additional direct negatives fail on6cb, preserved in
run15-inner-second-owner-before.txt (17 cases,13 pass,4 fail,zero skips):

- Candidate-reader snapshot Proxy and bundle getter each execute4 reads before
  rejecting. The snapshot JSON boundary must precede access to snapshot.bundle.
- Valid and invalid governor owners both reach context access when given a genuine
  owned new snapshot and a different authentic same-mode policy. Existing checks
  bind mode but omit the snapshot's policyDigest equality before consulting
  context. A deliberately throwing context-access sentinel proves only that early
  owner boundary failure; it supplies no fake successful lifecycle or metrics.

Root and author were informed before further checks or production corrections.
Use only the already approved narrow owner guards, preserving the restored original
v1 helper API and explicit new-v2 strict binding. Await a new frozen correction,
then rerun unchanged17. Every native, full-suite, history and overall finding
claim remains open. No additional check will run before this checkpoint is saved.

## Corrected seventeen-case direct oracle

After the complete red/green chronology was remotely saved at33311ff1 over fresh
f92d3, author froze correction639d3d34ebd24b4f54e509ade99cd7ce30d7fbe5. Applied
unchanged to own sourcea1528b4; production comparison with that frozen author
commit is empty. The unchanged expanded17-case oracle passes17/17, zero skips,
and unchanged original preflight again passes. Exact command/full raw is
run15-inner-second-owner-after.txt.

The candidate reader now rejects unsupported snapshot JSON before .bundle access.
Both governors retain explicit expected-mode checks and bind new-v2 policy digest
before any context access. The same narrow snapshot precondition was added to the
new task/verification/verdict protocol guards; surrounding method-bearing inputs
and PluginContext are not serialized. Original v1 helper compatibility remains.

This is partial direct owner acceptance only. It is not independent full Review
suite, genuine Core/ArtifactStore lifecycle, stored provider replay, or fresh
cross-locale producer acceptance. The earlier author's full-suite result applies
to6cb, not automatically to639. No package/finding integration is approved here.
Next: verify the author's exact durable639 checkpoint, independently reconcile
fresh MAIN metadata, and continue only specifically authorized remaining safe
checks. Keep all unavailable/flagged native boundaries and separate delivery
manifest work explicitly open; do not reroute a flagged action.

### Exact durable corrected source readback

Author639 is now backed by eba514b72993e4d4a3ef1e1aca08a3d59afbbe65,
sole f92d3f9f1bb68734d551c3196b02362bc682d5b9 parent, complete4,116-entry tree
ffa0439473012775eca2fc267fa906886be7c44c. This reviewer independently fetched
its remote commit/tree and compared every1,694 non-doc/non-test production entry
against the exact tested own checkout: all Git blob IDs and modes match. This
count intentionally excludes documentation and tests, including this independently
expanded17-case oracle; it is not a claim that the different review and author
documentation/test trees are identical. No new test execution was required for
this read-only source verification. Durable independent17/17 raw checkpoint is
8ea539d74381c53791762400aa73609eb61ae51b. Current work stops at partial owner
review; next authority remains completion of the genuinely required safe/native
gates, not automatic integration of a statically complete source package.
