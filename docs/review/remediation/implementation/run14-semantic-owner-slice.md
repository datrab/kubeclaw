# Semantic owner slice — explicitly incomplete

Base: fresh MAIN ff55ab962d16dc0eba5ee6aafa2150abaa723fb5, tree dcd8b5875bf08a414cbd2c9ebfbec52331486e26, exact local c3a3f9ceb0e287c93d62b2373ee2b7f49b86051d.
Source freeze: 534eb4f. Own run14-sdk-bundle-author, branch fix/resume-47-run14-sdk-bundle.
All 4,090 tracked file bytes and executable modes matched the fresh remote tree before edits. Independent Git objects; 62 workspace dependency links resolve inside this checkout. External immutable dependency hardlinks match the original current lock; no install or shared dependency modification.

This slice implements the independently approved e07c5c / be25cf bundle/governor/report owner map within the Review package, including explicit no-trap selector, initial/slice/expanded snapshots, versioned immutable byte checks, both governor builders, baseline-history pair validation, report-v3 and explicit Artifact encoding. Historical v1 bundle / v1 governor / v2 report constants and schema files are retained. The exported old partial-snapshot governor helper remains legacy only when its optional semantic mode is absent; explicit mismatched version pairs reject.

Compiler/recovery opt-in and Summary expected-pair binding are NOT implemented in this slice. No finding can close. Native old/new journal phases and the new independently executed locale consumer suite remain to be completed. Exact original selected unit scripts/schema check and full Nova types passed (raw run14-semantic-owner-slice.txt); that is not native acceptance.

## Genuine newly discovered prerequisite

The separately assigned run5_publish producer oracle ran actual original registered Core/Review lifecycle + repository/ArtifactStore under en/cs for gate, lean and audit. Its reported native result is six successful original producers but different original resolved policyDigest in every profile; lean/audit also produce different original simplification-facts/candidate evidence. Revisions, changed-manifest digest, context and selection are unchanged. Exact oracle/raw remote checkpoint is being finalized by its owner; this report does not substitute for those raw artifacts.

The approved design explicitly requires genuine fresh producer equality, not a fixture carrying precomputed inner hashes. Therefore this slice retains policy/simplification algorithms unchanged and is INCOMPLETE. A separately reviewed, narrowly owned policy + generated-facts/candidate extension must be agreed before touching those algorithms. Do not globally replace canonicalJson, change policy meaning, skip generated evidence, or disable lean/audit to produce a green result.

## Preserved preliminary author diagnostics

During first schema generation, the newly added portableReviewBundleSchema declaration was initially placed before reviewBundleSchema initialization. Actual error: ReferenceError: Cannot access 'reviewBundleSchema' before initialization at review-bundle-contract.ts:73. Moved only the new declaration after its original schema. Subsequent generation and check passed. A separate mistyped workspace command failed because @kubeclaw/nova is not a workspace; the actual full Nova tsconfig command above passed. These setup diagnostics are not product-gate results or hidden skips.

## Approved compiler API addendum (not implemented yet)

Root and independent reviewer examined original ff55 calls. Exported compileProject will keep fourth semantic argument omission as explicit legacy, preserving all original one/two/three-argument graph/config results. Only actual genuine new CLI --compile/new-run callers pass explicit source, report and semantic constants. Recovery restores all independent stored modes. Existing legacy-import authoring-only definitions remain exact old semantics; later genuine new CLI compilation can explicitly opt in. No inference from argument count, report encoding or source/run version; no wrapper required.

Next action: back up this incomplete slice remotely; independent reviewer runs unchanged no-trap red oracle against 534eb4f. Preserve helper native producer red, design/review only the newly proven inner owners, then complete compiler/Summary and genuine old/new production/phase gates on the fresh reconciled head. Frozen47 and PCR-SDK-001 remain open.
