# Original Review bundle: Czech semantic identity counterexample

Read-only inventory found a genuine remaining PCR-SDK-001 consumer on fresh MAIN 21b05129691b6c932fdf7fd2d12f906cb2736aad. No production source, schema, register, workflow or integration ref was changed. Exactly 47 original implementiert IDs still match frozen scope; SDK and overall completion remain OPEN.

The unchanged original test review-evidence-encoding-independent.test.mjs runs its actual producer/parser, strict input and bundle Ajv schemas, snapshotReviewBundle, storeReviewBundle and disk ArtifactStore. Its original --produce mode in en-US exits 0. Original --read in en-US exits 0. Original --read of the SAME persisted artifact in cs-CZ exits 1 at the existing snapshot digest assertion (line 70).

Stored digest: sha256:332c701d9c2896b952cda3f88bfc072c98871cb01a39bbdd567fa940677882f9.
Czech reconstructed digest: sha256:94f60f4c08a6a3706902e40fdf15effd73d968ccebaa8f6cdcef7d65836896d4.

Actual Intl comparison orders revisions fields base/changedManifestDigest/head in en-US but base/head/changedManifestDigest in cs-CZ. Closed ASCII field names therefore are not automatically locale-independent. This example needs no invented arbitrary field, Gateway, model, browser, DB, replacement adapter or fabricated receipt.

Seven owning source/test Git blob hashes were independently matched to the fresh MAIN tree before execution. The separate report candidate d4df6bfd6e84ac23d3e7095d1d8cfb609bc50d8c preserves storeReviewBundle byte-for-byte (function body checked against fresh remote source); snapshotReviewBundle and semantic bundle format remain unmodified. Its report-only outer Artifact encoding does not fix or authorize migration of this semantic bundle digest. The separate no-trap transport helper does not touch this domain either.

The sibling project-summary buildSummary/verifyFinalReview consumer reads exact original artifact bytes but still compares report.bundleDigest with sha256Text(canonicalJson(bundle)); it is an owning downstream migration dependency, not a separately executed failure claim in this proof. Historical semantics must remain explicit, not be silently rewritten on current recovery.

Reproduce using this checkpoint's run13-sdk-bundle-czech-probe.mjs with the selected current checkout path. This wrapper only invokes the existing committed test modes, records all subprocess exits and cleans its own disposable ArtifactStore. A wrapper exit 0 does not turn the captured Czech subprocess failure into a passing gate. Source and dependencies resolve within that checkout; no archived local Git object is required.

Next: independently review a bounded semantic Review-bundle cutover design covering genuine new producers, bundle/schema identity, graph/config provenance, report/governor/summary consumers and old pending/completed effects. Do not infer selection from reportArtifactEncoding, cache profile, source codec or locale. No production implementation is approved by this counterexample.
