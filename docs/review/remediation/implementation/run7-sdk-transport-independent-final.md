# Independent SDK transport package approval

Decision: APPROVED for this bounded transport package, conditional on exact fresh-head integration. PCR-SDK-001 remains OPEN. This is not an exact-47 completion or a provider/application acceptance claim.

## Frozen source and independence

Author local d2b6fd6d5949fe4f8425409ee542bc9bbd6e81e1, durable remote bade491d2c20e0efc5b33816d447bcb187f790d0 on fix/resume-47-run7-sdk-final-d2b6fd6. Remote parent freshly verified a43aa256bdce35d7f9c44d5d661e59c4055e562e; tree f0f056cdc266f83759e9e32807385894ea4e1a1f. All 2105 non-documentation blobs, modes and types independently match tested reviewer source f1d60a8f18082385ed42e021964559ad8d396fe0. Reviewer adds only its independent test and evidence, not production code. Current Reader, Adapter, coupled Product/controller/chart, dispatch-projection and Envoy sources are preserved.

Own checkout: run7-sdk-transport-review. Immutable external dependency cache is hardlinked from the current root cache, not physically independent bytes. Package-lock blob 96cdba80650e16fb5699df314516b14d08931b95 matches current source. All 64 declared workspace links resolve to this review checkout; no source links to another checkout and no dependency install/modification. Tests use ephemeral local files, original processes and loopback HTTP. No CI, deployment, production, paid resources or third-party messages.

## Resolved review defects and contract

The rejected d545 model-payload control field collided with legal historical model data; both string and const-shaped counterexamples remain in run6-sdk-profile-collision-independent evidence. The closed invocation/effect envelope relocation preserves that model key in actual HTTP/HMAC bytes and replay. The unchanged independent test also exposed the ec994 strict-Ajv compile failure; bc015 fixed schema declarations without changing strict settings. Earlier failures and the original retirement writer-version assertion are preserved, not hidden.

Only genuinely new runs write run-snapshot.v3 with a required finite canonical generated profile. Existing v1/v2 runs retain exact absence/legacy transport semantics. New snapshot shape/profile/digest are validated, including honestly rehashed invalid/future/downgraded contracts. Generic graph snapshot nodes retain the original Unicode graph API; no weakened original pipeline validator. The Runner retains the canonical frozen profile reference. All 11 current dispatch producers receive original Core context before journal admission, with identity/dependency/confidential propagation. Control stays outside model/cache/budget/wire/HMAC.

Two historically exported invoke-only Review helpers retain that original API; only those two optional-contract paths have legacy absence behavior. Explicit malformed profile fields still reject. Their original tests are unchanged; additive actual registered HTTP/HMAC cases cover absent/current/undefined/future/null/extra profiles.

## Independently executed final gates

- run7-sdk-transport-independent-final.txt: 51/51, zero skips. Actual archived v1/v2/current v3 runPipelineV2 through original architecture stage and registered ACP; nine version-by-requested/accepted/completed SIGKILL boundaries through original journal and registered HTTP; exact persisted phases, original prefix preservation and watchdog distinction; en/sv ACP transport-key comparison; corrected original exported helper HTTP cases; unchanged reviewer HMAC collision, metadata conflict and completed replay; strict profile/schema/budget cases; current real dispatch projection/retention/read-fence and retirement gates.
- run7-sdk-review-helpers-independent.txt: four original Review jobs/verification/audit/revalidation scripts exit 0.
- run7-sdk-legacy-independent-final.txt: actual archived historical CLI probe from this current checkout, then 12/12 original SDK/ArtifactStore/FileEffectJournal/identity/source/recovery tests, zero skips. Exact archive bytes are used without historical local Git objects or dependencies resolved from another checkout.
- run7-sdk-types-independent-final.txt: actual owning Foundation and Nova TypeScript projects, canonical SDK generator check and diff check exit 0. Earlier reviewer invocation of a nonexistent package script is explicitly preserved in run7-sdk-type-command-diagnostic.txt, not a source defect.
- Earlier unchanged 41-test original dependency/SDK/source/retirement regression and red/green HMAC evidence remain in run6-sdk-profile-*; prior fresh-1d native 15/15 evidence is separately labeled prerebase, not substituted for final results.

Counts above are tests, never findings.

## Boundaries and remaining work

The ACP receiver deliberately returns real HTTP 503 and is not an OpenClaw Gateway. This proves native outbound identity/profile and fail-closed journal behavior, not successful model execution. The generic HMAC receiver genuinely verifies bytes, but is not application acceptance. Accepted-without-receipt recovery remains unresolved and performs no repeat HTTP. The archived CLI correctly refuses an already terminal old run after preserving its original graph; that is not a successful pipeline run. Projection fixtures explicitly report original provider sandbox not built and make no provider acceptance claim. Confidential mode remains explicit, unjournaled and without an exactly-once guarantee.

No genuine producer emits a legacy effect hash together with new closed profile metadata; the existing identity reader can accept an honestly constructed combination. This is not a demonstrated compatibility failure. A global portable-ID schema restriction would incorrectly reject legitimate confidential UUID identities and is not silently introduced.

Confirmed separate admitted Review cache defect remains OPEN: durable checkpoint 05faa821ad473135654d64702170d7835f5fa661, run7-sdk-cache-context-checkpoint.md and its actual Core/ArtifactStore/FileJournal probe. Valid contextRequest assessment keys I/i cause same persisted cache proof to pass en-US and fail tr-TR. Do not remove valid accepted fields to hide this. Outer exact Artifact bytes alone do not fix two inner cache digests. The separate Prism archive locale audit is also not repaired by transport.

Next action: root integrates only the exact independently approved transport source plus raw/review evidence against a freshly read main head, with exact-parent fast-forward update and affected retests if head changes. Keep PCR-SDK-001 and the exact-47 task open; review and implement the separately owned cache/other persisted-domain versioning packages afterward. This review-only backup does not itself contain the author production package.
