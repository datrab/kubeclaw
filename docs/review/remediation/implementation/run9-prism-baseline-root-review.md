# Root: independently reviewed Prism baseline codec

Bounded source approved and root-tested on top of SDK integration
28909f62da68f70d94e0672ce7ae12ced8d7b3d9 (local69253f2).
Author source60a03f8cb86b45f397aa144b2dc058a58cce5f3e is backed at
1ca9a7236bc8bec40ce403b21b6f5b53dad275f3; independent review91ed5be is
backed at4d09c0a9c4e925eba73c5656508e38f4d067a22d. Root read both
reports and the complete owning source diff. All3852 local review entries
matched the fresh remote tree. Its three extra final author report/raw files
were imported and independently match their remote Git blob hashes exactly.

No production conflict on SDK integration. Root source3d5b0b5 plus documentation
7c40052 preserves all SDK profiles, current coupled Product/controller/chart,
Envoy, dispatch and source authority. The registered Prism reader stage remains
unchanged by this package. The original v1 schema and Control prior-publication
return preserve old approved bytes; only genuine new publication selects paired
archive/manifest v2. New checksum preimage binds the explicit checksum domain
and UTF-16 encoding. No old approval is retagged, no locale fallback, no new
browser dependency in the contracts browser index. Shared Worker Core and role
engines remain unchanged.

Completed on root's integrated source:

- Original Prism contract/remediation programs and five new tests, zero skips:
  actual production assembler, original CAS and Nova importer; all16 en/da/tr/sv
  combinations, exact historical v1 bytes and preserved cross-locale denial,
  23 corruption cases, eight coherently hashed semantic negatives, quotas/path
  bounds and reordered checksum keys.
- Independent coherent v1-to-v2 and v2-to-v1 paired retags both reject without
  changing original CAS or approved digest. Original archive-integrity passes.
- Original Control/storage/engine/renderer/pipeline suites: 25/25, zero skips,
  retaining their original fixture scope; PGlite is not native PostgreSQL pool.
- Contracts, Prism and prism-design TypeScript projects exit0. Configured lint
  on changed bounded owners/generator/tests exit0; generated validator bytes
  remain exact. Raw outputs: docs/review/evidence/run9-root-prism-native.txt and
  run9-root-prism-gates.txt.

Full Control monolith retains25 preexisting lint errors (complexity204 to202),
documented in exact author and reviewer raw files; no full-lint pass is claimed.
Real PNG/CAS assembly is not a new browser capture, actual DB/HTTP publication,
human approval or application delivery. Those original gates remain open.
PCR-SDK-001 stays partial pending cache/report and registered reader packages;
frozen47 remains8 verified/39 incomplete. Publication requires fresh exact parent
28909f62 and ff-only updates after all source/tree/ref/workflow checks.

## Supplementary probe reproducibility correction

The publication reviewer caught local-Git-object lookups in the supplementary
retag script before publication. Complete original script is preserved byte-for-
byte as run11-prism-baseline-independent-historical-audit.mjs.txt (Git blob
a15f58287eb708733efe902f2e21a510c111a9e6). Its old static audit remains historical
evidence, not a portable native gate. The current executable drops only that
static prefix/import; root byte-compared the entire native CAS assertion body
and found it unchanged. No native gate or assertion was weakened.

Root then exported exact c5018c5 without .git, rebuilt all64 workspace links
within that export and made git unavailable (actual spawn ENOENT). Both genuine
coherent retag denials passed with unchanged original CAS, exit0. Raw evidence:
docs/review/evidence/run9-root-prism-git-free.txt. The five primary historical
producer/locale tests already use archived original bytes, not local Git objects.
This correction changes only supplementary test reproducibility, no product code.
