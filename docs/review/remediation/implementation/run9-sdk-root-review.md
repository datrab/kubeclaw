# Root approval: bounded run-frozen transport and original supervisor start

The bounded transport source is independently approved and root-tested. This
does not close PCR-SDK-001, PCR-SCAFFOLD-OPS-001 or the frozen47. Eight findings
remain verified and39 incomplete. The misleading older SDK verification label
is explicitly corrected to partial while preserving its implementation status.

## Exact reviewed source

Author d2b6fd6d5949fe4f8425409ee542bc9bbd6e81e1 is backed at
bade491d2c20e0efc5b33816d447bcb187f790d0. Independent review
2fd31438373d6052e761c8349943e0d65b092ab0 is backed at
898fada24b84fddb6eff8c5f9a434731a8054583. Root source685ea61 has
byte-identical skills source and final author report/raw files; the independent
test is additive. Supervisor test matches author32a9c2 exactly and has a separate
root review in run8-supervisor-root-review.md. Earlier root resume docs win over
stale review-branch checkpoint content; no production conflict was resolved by
guessing or copied over current Envoy/dispatch/coupled sources.

New runs select required snapshot.v3 closed transport metadata; old v1/v2 retain
exact original absence and wire behavior. The control marker is not model data.
Original exported invoke-only Review helpers retain their API. Historical effect
requests, source authority, dependency ownership and confidential boundaries are
not silently retagged. Worker Core and role engines remain distinct.

## Completed root gates

- run8-root-sdk-integrated.txt: 64/64, zero skipped/cancelled/failed. Genuine
  original v1/v2/v3 run producers, registered HTTP and all nine phase-specific
  SIGKILL histories; actual model-key/HMAC compatibility, strict profiles,
  source/effect/CLI recovery, dispatch projection, fenced reads and retirement.
- run8-root-sdk-original-gates.txt: original archived historical CLI, owning
  Foundation and Nova TypeScript projects, canonical generator and complete
  original Review package suite all exit0, including million-line compiler,
  original Core/Git/ArtifactStore paths and cumulative coverage.
- run9-root-sdk-fresh.txt: own fresh checkout repeats 5/5 original supervisor,
  profile and independent HMAC tests, then the actual archived CLI, exit0.
  All64 workspace links resolve to this checkout; third-party cache bytes are
  immutable hardlinks after identical lockfile verification, not fake independent
  dependency copies. The archived producer resolves current checkout sources and
  dependencies without old local Git objects.

Source whitespace validation is scoped to production/tests/scripts. Historical
raw output is preserved exactly, including warnings, whitespace and earlier
failures. Original Prism stage complexity16 versus limit15 is retained in the
author baseline lint evidence; no blanket full-lint pass or weakened rule.

## Honest limits and durable next actions

Native HTTP503 receiver is not a successful OpenClaw Gateway/model. Actual built
sandbox EPIPE/init70 is not a passed provider gate. Accepted unknown effects stay
unknown without repeat HTTP. Historical terminal CLI refusal is the expected
compatibility assertion, not a full pipeline success. Original restored-platform
supervisor start passes; real process-adoption status-failure acceptance still
requires readable child procfs cmdline.

Remaining SDK domains have actual admitted counterexamples and durable packages:
Review cache v4/v2 WIP3872c37 with independent getter red fabe2c7;
original report digest-map replay4ac681d; Prism baseline author1ca9a72 and
review4d09c0a (not yet root-integrated); replay-safe Prism reader design49211bf
with implementation separately authorized. Operator projection d6ce451/ba13e99
is independently reviewed but requires current SDK reconciliation before root
integration. Broader OBS/native gates stay open. Full refs and next actions are
in resume/run5-active-packages.json.

Publication must use the freshly read a43aa256bdce35d7f9c44d5d661e59c4055e562e
as sole remote parent, verify all final blobs/modes/types, and update only
fast-forward after rechecking head and workflow branch filters. Head movement
requires reconciliation and affected retests. No CI, deployment or third-party
message is authorized by this approval.
