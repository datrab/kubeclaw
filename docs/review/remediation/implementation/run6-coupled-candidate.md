# Fresh coupled candidate after Reader and Adapter integration

Source freeze: `78ef08eaae0c8fcbfc6412d4c0f0f93113caec81`.
Exact base: remote `b06c06dce3364c482a1da5637c36bc2b14ba8c72`, local
`ca00dc72893deaae21d66f1b302962f97638f7ae`, tree
`78f4ce6ebccc20f5dd00817d7349d68bb64ece60`.

Fresh mandatory resume documents, current register and original requirements were
read again. Exactly 47 IDs were independently derived from original `c38779c`
status `implementiert` and matched current `partial-47-scope.json`, unchanged.
All 3,448 current baseline blobs/modes/types were matched against the freshly
read remote tree. All 249 saved candidate delta paths were separately matched
against remote package `7bfa6bbf467e56c2191a1dd4ed44c43cc80daa85`; 3,432 untouched
current-baseline files remain exact, preserving all new shared Reader and Adapter
changes. Original Product43/Controller20/Chart10 source deltas have no conflict.
Manifest: `docs/review/evidence/run6-coupled-reconciliation.json`.

Fresh author results on this actual combined source, all ultimately exit 0:

- Original Product/Control HTTP/SQL/session/TLS suites: 21/21, no skips.
- Original complete Go controller suite: 55 top-level tests, no skips, including
  TypeScript producer interoperability from this current checkout.
- Original Controller + Product Chart acceptance: 6/6, no skips.
- Full genuine native Kubernetes/etcd ServiceAccount/RBAC/VAP, CRD/CEL, schema
  retention/count/bounds, signed original-controller transition and real ACK-loss
  matrix passed; no skips, both native children reaped.
- Current shared blob-reader original and independent boundaries: 6/6, no skips.
- Prism typecheck, original focused Product lint and Go vet: exit 0.

Initial concurrent native/Product attempts hit a genuine global disk-full event
(ENOSPC, PostgreSQL53100); the concurrent reader test timed out under that same
resource pressure. All initial raw outputs and cleanup logs are retained in
`run6-initial-*.txt` and `run6-coupled-native/`; these are failures, not approvals.
No source or assertion was changed. After reclaiming only this invocation's
three newly extracted duplicate test executables, native, reader and Product
were rerun sequentially using an owned TMPDIR. Product test-file concurrency was
one; every original intra-test race, request and assertion remains unchanged.
Final raw outputs are `run6-final-{native,reader,product}.txt` and the full
`run6-native-final/` tree. Native assets are the freshly checksum/release-metadata
verified original v1.35.0 archive, read-only from the prior verified extraction.
The initially removed duplicate extraction must not be used as a future path.

The previously reviewed proof boundaries remain unchanged: the namespace fence
uses genuine alternate controller-SA tokens and original chart RBAC; the signed
controller's outbound native API client is the private privileged test identity.
Ingress status and delivery receipts used to set up controller admission cases
are explicit fixtures, not Tailnet connectivity, an actual message, app login or
human operator acceptance. No workload, deployment, existing kubeconfig, CI,
production mutation or third-party message was used.

Next: independent review of this fresh combined candidate and its actual native
retest, then root-only integration against a freshly reread matching head.
Only root may update the register or decide IFR-18 closure after that integration;
F-T14/T01 delivery and all-47 completion are not established here.
