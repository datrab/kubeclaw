# Early independent semantic bundle review checkpoint

INCOMPLETE; the first author source slice has been applied for independent review,
but the semantic package is not approved. Root-approved design e07c5c07/be25cf is
not implementation acceptance.

Own new standalone run14-semantic-bundle-review was reconstructed from fresh
remote MAINff55ab962d16dc0eba5ee6aafa2150abaa723fb5, exact4090-entry tree
dcd8b5875bf08a414cbd2c9ebfbec52331486e26. Mandatory remote resume files/register
were read and original baseline fetched again: exactly47 matches frozen scope.
All62 locked workspace links resolve relatively inside this own checkout.

The independently authored permanent test review-semantic-selector-independent.test.mjs
uses the existing original archived bundle fixture and original report/governor
shape. Unmodified original legacy JSON input still passes both owning readers
and preserves the original immutable bundle digest.

Six direct original-admission negatives fail on this baseline: bundle Proxy32
traps, bundle schemaVersion getter1, nested revisions Proxy10; report Proxy30,
report schemaVersion getter1, nested governor Proxy12. The test requires zero
traps and the original boolean/parse-result rejection contracts. This is the
explicitly approved no-trap boundary in the design, not an unapproved production
scope expansion. Raw exit1 is preserved; these are direct owner unit checks,
not an actual Core lifecycle, genuine model or provider proof.

No production file has been edited by this reviewer. The author owns the complete
semantic implementation; helper authors own separate native producer fixtures.
Next: receive coherent frozen source, review selectors/schema pair/graph recovery
and rerun these exact tests unchanged. Then independently execute the actual
Core/ArtifactStore/FileJournal and original producer/reader/CLI gates in a
coordinated test slot; preserve any new red evidence and checkpoint promptly.

## First frozen slice and new builder rejection

Exact author source 534eb4fa7321b0879de7774f4aa91370097de910 was applied to this
own standalone checkout, reviewer commit 739c704. The unchanged seven-case
admission test now passes 7/7 with zero skips; complete raw is
run14-semantic-selector-independent-after.txt. The local fetch required an
explicit shallow-root update; its missing historical ancestor diagnostic is
preserved, not represented as a restored old repository history.

An additional independently constructed original report-builder positive uses
a genuinely owned legacy snapshot, original resolved gate policy and original
governor fixture. It passes. Three new unsupported bundle-selector inputs fail
the independent rejection gate: Proxy executes three traps, schemaVersion getter
executes once, and review-bundle.future silently constructs accepted report.v2.
Expanded suite: eight pass, three fail, zero skips, exit one. Exact raw is
run14-semantic-builder-independent-before.txt. Root and author were informed.
This is a direct exported owner boundary, not an observed native stage bypass.
Correction must reject before reflecting the bundle and explicitly handle its
supported version; it must not JSON-validate the entire method-bearing input.

Compiler, Summary, actual fresh producer parity, owned policy/generated evidence,
history and crash-replay gates remain incomplete. The helper's cf45e370 native
old-producer counterexample remains red evidence only; no candidate validation
or external model acceptance is implied. No attempt is made to reroute a flagged
helper operation.

## Corrected builder and direct Summary parity

Exact author 1595f98002fa0d0217c1d107810d177c688a31a0 was applied unchanged,
reviewer source 89c59d54840eff678c7f41f321626e37a49c9201. The expanded eleven-case
oracle now passes 11/11 without skips; raw run14-semantic-builder-independent-after.txt.
This correction validates the snapshot JSON value before bundle selection and
rejects unknown present tags, while retaining original absent legacy helper input.

Four additional direct cross-owner tests in review-semantic-summary-independent.test.mjs
pass without skips: strict Ajv original/new bundle and report schema positives;
explicit Summary expected modes, unknown/mixed pairs and corrupt governor hash;
historical both-unversioned branch; invalid selection and zero Proxy/getter traps.
Raw run14-semantic-summary-independent.txt. These fixtures deliberately construct
the new governor identity for schema parity only, and use the still-legacy policy
resolver. They are NOT proof of native new policy/governor production or storage.

Source review confirms compiler fourth-argument omission stays legacy, NEW CLI
explicitly opts in, cumulative Summary tag is conditional on final Review, and
recovery compares the complete graph after separate source/report/semantic mode
reconstruction. Genuine CLI and all module/final/neither recovery cases remain
to execute independently; source inspection is not a passing native gate.

Fresh remote metadata-only MAIN ef5399be16c58eb6b8cf0398e2e183c1226d27c9 changes
only run-20260910t021800.md and run5-active-packages.json from ff55, confirmed by
the exact remote tree comparison. The next review-only
backup preserves that newer metadata and does not overwrite it with this checkout.
