# Root approval of the coupled Product/controller/chart package

The independently approved source is `78ef08e`, with complete author checkpoint
`38aaf5c` backed at `d9b9a42d1ae3f2e3f3ea8795f93b5e81c03edfdf`. Its exact
parent basis is the fresh repair head `b06c06dce3364c482a1da5637c36bc2b14ba8c72`.
Independent final review `65dca0f`, including raw native output, is backed at
`6ff6061e08e371c1b92107496447d03dd190c079`. This review is of the complete
combined source after the new Reader and Adapter packages, not an earlier tree.

Root inspected the coupled authority boundaries and original IFR-18 requirement,
the source manifest, all 73 exact original package blob hashes, the native
API/TokenRequest/TokenReview/RBAC/VAP and signed-controller test implementation,
and the independent final source/provenance report. The Product wrapper retains
the original ControlService composition. Shared Worker Core and role Engines
are unchanged. No broader success is inferred from a local adapter or fixture.

At root source `b16d9d2f8b4c68afe5702a9c080f0ffff768d520`, every production and
test source matches reviewed author `38aaf5c`; the only differences are three
root checkpoint documentation files. Root ran the following original checks:

- Full real Kubernetes 1.35.0/etcd control plane: actual alternate-controller-SA
  tokens and original chart RBAC/VAP, allowed Namespace CREATE201/DELETE200,
  wrong-prefix/missing-label/control-namespace DELETE422 policy denials and
  unchanged control objects, in two sequential alternate configurations.
- Original CRD install/Established, structural preservation, native CEL immutable
  append-only receipt history, count128/129 and individual maximum-field bounds,
  exact replay and stale resourceVersion rejection.
- Original signed controller with actual native API persistence, invalid authority
  and expiry cases, exact receipt replay, extension/stale cleanup and a real
  committed PATCH whose acknowledgement is dropped, then recovered without a
  second write. The two temporary native child processes were reaped.
- Product/Control real HTTP/TLS/session/SQL suites: 21 registered executions,
  17 distinct cases because four original imported composition cases register
  twice; all pass, no skips. SQL is PGlite here, not a native pg.Pool claim.
- Original Controller/Product Helm regressions: 6/6, no skips; complete Prism
  typecheck exits 0. Author and independent reviewer also passed all 55 original
  Go top-level tests, current reader cases and focused lint/vet at this source.

Root commands and complete raw outputs are in `docs/review/evidence/`:
`run6-root-coupled-native.txt`, `run6-root-coupled-native/`,
`run6-root-coupled-product.txt`, `run6-root-coupled-chart.txt`, and
`run6-root-coupled-types.txt`. Earlier ENOSPC failures and unchanged-assertion
successful retries remain in the author/reviewer evidence; no failure was erased.

## Finding decision and boundaries

**IFR-18-001 is verified in its original scope.** Admission and controller identity
come from the same original Helm values, and the explicitly required real native
alternative-SA regression is independently and root reproduced. A later installed
legacy-policy migration remains subject to its documented operational procedure
and separate deployment authorization; it is not an invented extra original test.

The coupled gate is no longer blocked. This does not close T01-F02 or F-T14:
actual application login, Tailnet access, recipient delivery and authenticated
human acceptance/extension remain their original separate requirements. Seeded
Ingress status and delivery receipts in controller admission tests are fixtures,
not proofs of those events. Signed-controller outbound native API uses private
test-admin credentials; only the separate namespace-fence path claims actual
controller-SA RBAC. No workloads, existing kubeconfig, deployments, production,
CI, paid resources or third-party messages were used.

Frozen scope remains exactly 47: **eight verified, 39 incomplete**. Overall
completion is false. Continue the independently owned SDK transport profile,
causal dispatch projection and native Envoy packages from their remote checkpoints.
