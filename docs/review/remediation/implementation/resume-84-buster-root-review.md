# Root integration review: Buster shutdown and durable failure ownership

Reviewed source daa2b37 plus36e2027, integrated locally as7d3a1d3 and5c0f636. Original Buster service bytes before these changes matched the author's baseline. No shared SDK, WorkerCore, NovaCore, role capability or provider implementation was changed.

The original service lost rejected execution ownership after removal from its active map, and ignored rejections during shutdown. The correction retains the first escaped execution error before releasing that map owner, disables new admission/starts through the existing stopping state, and preserves all durable job records. A shutdown snapshot includes already pending durable admission. It waits for all captured participants, then propagates the retained execution error or another genuinely rejected participant. It does not reinterpret ordinary structured BuildKit readiness results as shutdown failures.

Root reviewed the five regressions against the actual FileBusterPlanJobStore, signed committed Git source, registry/plan constructors and real quota/lock boundaries. Invalid runtimeRoot causes the original filesystem helper's failure before provider startup; terminal-record size then genuinely exceeds the configured store quota. The blocked-admission case holds the original durable admission lock and requires the public accepted response and stored accepted record after unlock. Its small scheduling allowance cannot produce a false pass if admission never crossed the stop check: the accepted assertion then fails.

Root executed the actual existing `npm run verify:test-gate:remote-plan` with the new regression appended: original remote-plan contracts, contract TypeScript, Nova TypeScript and **all five storage/shutdown cases pass**, zero skips. The new test has a persistent `tsconfig.buster-shutdown.json` extending the original Buster compiler configuration, and that exact config typechecks. The root `typecheck:integration` script now includes it. Every pre-existing command and compiler option is preserved. Knip configuration consistency and runtime-package ownership pass; full Knip was not run.

Raw Root outputs are in `docs/review/evidence/resume-84-buster-root/`. The author's four pre-existing Buster lint categories remain; no rule override or baseline expansion was introduced. Independent final review08e8d0f confirms6/6 cases (the five new cases plus original reservation), types0, and the same explicit remaining boundaries on exact author source36e2027.

This is not proof of native provider execution, process-tree quiescence, restarted orphan ownership, or full PCR-BUSTER-ENGINE-004 acceptance. In particular, the existing status-read suppression inside #execute remains a separate unresolved source boundary. Preserved running/accepted records are not silently rewritten into terminal states, and no input/evidence/history cleanup authority is added.
