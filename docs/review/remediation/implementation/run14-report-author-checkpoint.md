# Report artifact codec cutover — author checkpoint, independent completion pending

Fresh baseline: `21b05129691b6c932fdf7fd2d12f906cb2736aad`, exact local `07d35b6978611ac0a4095cee8ecc3196419870c8`, tree `268ca86b318b965ddb285cee6e093d4d98068ec6`, 4,024 blobs. Mandatory remote resume/register/original PCR-SDK-001 requirements were reread and the original frozen 47 IDs compared unchanged. This package does not close PCR-SDK-001 or the 47.

Current source/test freeze: `473444398c1ee18331edea88952dde49bb10853a`. It reconciles the exact prior report candidate `d96b87449b505c31b2bd06fd507cab4d673a4d01` (remote `2ae7f59cdc6d1a976f2de9e50f06139982abed7c`) against integrated cache-v4 and registered Prism reader. Only the report-owning selector/producer and project compiler/recovery are changed. Cache profiles, bundle writer, report semantic version/item IDs, original absent-mode put_json payload/ordinal, whole-graph and package-pin verifiers remain unchanged.

New compiler calls explicitly select portable module and final report artifacts. Stored report mode derives from the exact expected generated Review node set, independently from stored source identity. Mixed/unknown modes and missing/extra/mistyped Review nodes reject; arbitrary stored configuration is never adopted. Historical cumulativeStages omitted argument remains legacy because the real archived compiler imports the current coverage helper. An actual archived compiler test proves portable source plus legacy module/final reports remains recoverable. Authored project Review config cannot override the compiler-owned selector.

Actual original builder and strict report schema produce legal `sha256:aa...` / `sha256:af...` item-map vectors whose legacy bytes differ under Danish collation. New report bytes and original completed receipts are identical across 16 native en/da/tr/sv producer/consumer pairs. The archived complete original storage module executes from checked-in bytes (Git blob `d0f6184b60b521b5c82e22ff97896362935ecb67`), resolving dependencies from the current checkout, not old Git objects. Legacy same-locale replay remains exact; legacy cross-locale replay retains the original fail-closed outcome. Requested/accepted prefix tests retain exact original persisted bytes without rehashing; these are prefix-replay proofs, not actual process crashes.

Strict reports are stored through actual Core/ArtifactStore and consumed by the actual governor and project-summary readers across locales. Supporting gate decisions are explicitly artifact-contract fixtures, not provider execution. Existing registered Review/Git/HTTP-control tests now additionally run with portable report config and verify all stored report tags/bytes while bundle tags remain absent. Their HTTP control is not a native model or gateway proof. Direct corruption tests start from a genuine CAS response and reject wrong mode/reference/owner and non-JSON before getters execute.

Independent reviewer additions were imported byte-exact from `fc5528409aae12b26eae5abb1597566ac5e4961b`: author-domain negatives and two real SIGKILL cases. Those hold the ORIGINAL durable RecordStore fence, wait for the actual Core accepted event, kill the actual writer, and prove restart uncertainty/no replacement metadata/blob. Their reviewer checkpoint is explicitly incomplete until fresh-head gates finish.

Author checks on the earlier exact candidate passed: full Review/Nova types and configured ESLint, original full Review suite, original archived CLI terminal recognition, original phase6 package-pin rejection/explicit-upgrade checks, corrected report posttest14/14 plus registered live case. The phase6 evidence is the original real Core/package contract test; it is not an authorized upgrade of a historical Review installation. Old pinned package checks were not bypassed.

Fresh integrated21b full `npm test -w @kubeclaw/plugin-review` completed exit0: original scale gate43,738ms under its unchanged45,000ms assertion; all original suites and corrected new posttest14/14 plus portable live case passed. The first full npm wrapper on the earlier candidate timed out while bad-cwd posttest failed; the reviewer-found correction changed only the NEW test command's cwd, not original fixtures/assertions. Failures and partial/complete captures are retained in `docs/review/evidence/run13-report-initial.txt`, `run13-report-before-reconcile.txt`, and `run14-report-npm-partial-capture.txt`. The latter is explicitly a partial tool capture, not an untruncated full raw record.

Next action: finish independent fresh21b full suite/CLI/pin and real SIGKILL review, obtain complete native matrix raw, repeat author affected types/lint/CLI under the orchestrated quiet-test slot, compare final immutable source hashes, then request root integration against a freshly verified head. No MAIN update, CI, deployment or external coordination was executed. All current checkouts are private shallow snapshots; required source objects and exact trees are self-contained after the inherited old alternate became unavailable. No historical Git ancestry is needed by acceptance probes.
