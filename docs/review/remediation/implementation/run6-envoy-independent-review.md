# Independent native Envoy review

Status: incomplete, pending expiry-fixture correction and re-execution. No finding closed.

Fresh remote main b06c06dce3364c482a1da5637c36bc2b14ba8c72 and candidate fe88490266175c91c0eb624a1c3a63b5f7d42462 matched all 3,448/3,458 local Git blobs, modes and types. Mandatory remote resume files and original IFR-25-001 / IFR-07-001 requirements were read; original c38779c register yielded the exact unchanged 47 IDs. Own isolated checkout tests frozen 70f3592bbac74d48d039e3c67b82b6db14191f24.

Independently fetched official Envoy v1.39.0 release metadata and matched executable SHA256, size and actual version. Full native run exits 0: six unmodified original rendered configs validate with genuine Envoy; real filesystem SDS installs exact serial/SAN in both contexts across five transitions, readiness fails/recoveries occur while health/bootstrap remain 200 and the process remains alive. Admin isolation, session reuse zero and bounded stderr failure event assertions pass. Original chart tests 2/2, deployment truth, SPIFFE contract and configured lint pass with no skips. Original retained production chart patch reverse-applies unchanged. Full raw and provenance are in run6-envoy-independent-* evidence.

Review correction requested: openssl -days -1 produces a reversed validity interval (installed valid_from > expiration_time), not a previously usable expired SVID. Preserve this passing baseline but do not claim the named actual-expiry proof. Author must generate explicit past notBefore < notAfter < now, assert that interval in /certs, and rerun independently. No production source defect found.

Architecture review: same scoped helper in both charts; exact own workload SPIFFE URI on both mTLS directions; mandatory client certificate; original SDS source/secret names; fresh connection and disabled TLS session resumption; no business/admin route reachable from readiness listener; bounded self-check and failure event fields; original business peer allowlists preserved. Chart runtime references remain pinned. Native test isolates only rendered self-check listeners, using actual filesystem SDS; all full original configs validate first without alteration. Local test loopback binding is explicit. Actual SPIRE gRPC/UDS/workload attestation, Kubernetes readiness/rollout, BuildKit/DB fault acceptance and operator alarm delivery remain untested/open. stderr event emission is not operator delivery. No production/deployment/CI actions.

Next: author corrects expiry fixture, independent reviewer reruns frozen updated source, then root reconciles six chart and three test deltas with newly integrated coupled Product/controller/chart source and repeats affected original checks before any integration. Root owns register/closure; IFR-25 and IFR-07 remain incomplete.
