# Independent corrected Envoy readiness approval

Status: approved for bounded source integration on the inspected 4f70d8f baseline; neither IFR-25-001 nor IFR-07-001 is completed.

Fresh remote main 4f70d8f13e282e01d014fe82557d37c90d52cb71 exactly matched all 3,773 blobs/modes/types at local3ea43f497d3c18445adf89a4346718185dc5b00a. Mandatory remote continuation documents and original requirements were reread. Independent final checkout tests author608a33d68ac12573553e92c6a5161c349abc1e77, reviewer51bc2bf, identical full tree453876f11cc2c4da62818d8a9a2468878b160bed. Existing independently integrated Product/controller/chart sources are preserved; the overlapping Prism workloads diff is only the worker-trust container probe block. No shared Worker Core or role engine architecture changed.

Both independent findings from run6-envoy-independent-review.md are corrected by the author. Original Helm now rejects trust-enabled default/explicit false or unsupported existing-name cases with a precise prerequisite error. Trust-disabled original default behavior is unchanged; all five actual named-SA consumers match their exact rendered workload identity. Current chart supports no legitimate existing-SA override path to preserve. The check does not widen trust to the default SA.

Expired SVID is now signed by genuine OpenSSL CA with explicit valid_from 2026-09-07T23:45:50Z < expiration_time 2026-09-08T23:45:50Z < test time. Both original Envoy TLS contexts show serial1000 and exact expected URI. This fixes the former reversed interval; baseline evidence is retained. Readiness is additionally rechecked after each exact installed-certificate observation, excluding a transient pre-install status as the sole evidence.

Independent execution: genuine official SHA-verified Envoy1.39.0 validates all six complete unmodified rendered configurations. Functional actual Envoy/filesystem SDS sequence is valid200, installed wrong-SAN503, recovery200, installed genuinely expired503, recovery200. /health and /bootstrap stay200 with the same process alive; /admin on health port404, actual TLS session reuse0 and bounded stderr failure event checks pass. Envoy subprocess is reaped and temporary credentials/configs removed. Full raw is run7-envoy-independent-native.txt. Helm trust3/3 plus original Product decision chart3/3, deployment truth, SPIFFE contract, canonical ESLint all exit0 with no skips (run7-envoy-independent-checks.json).

Review of the six chart and three test deltas finds no remaining blocking defect at this scope: exact own identity in both directions, mandatory mTLS, original SDS secret/transport source names, fresh connections/session resumption disabled, bounded self-check without business/admin routing, separate liveness/bootstrap/operational probes, existing peer allowlists and coupled Product authority preserved. Production helper is identical across the two chart scopes. Changes are cause-level configuration, not a shim, substituted Envoy or weakened test.

Remaining native gates are explicit: filesystem SDS is not SPIRE gRPC/UDS attestation; original full config validation is not actual Kubernetes rollout/readiness propagation; stderr event production is not delivered operator alarm. Real BuildKit/DB fault gates and IFR07 release update/old-peer rejection remain open. Do not close these findings. Parent must verify final remote source hashes, reconcile any newer main head and rerun affected tests, then integrate fast-forward with exact freshly read parent. No deploy/production/CI/paid-resource/third-party-message action occurred.

Follow-up original rollout fixture: author adb4934de8ecc132b6e9ed3ec23b7c8edba9c584 adds only serviceAccount.create=true to actual Kubeclaw Helm inputs, preserving every existing checksum/config/LiteLLM assertion. Reviewer inspected and applied it as402a676, independently reproduced the precise precondition failure before that change, then original check-rollout-health.mjs and canonical lint passed exit0. Raw before/after is run7-envoy-independent-rollout.json. Production source remains608a33d; no rollback of the dedicated-SA negative tests. A scan found no other Kubeclaw SPIFFE render missing that fixture prerequisite. The script explicitly reports liveRollout:false; this is not native rollout completion.
