# Run 20260909t2220: fresh reconciliation and continued repairs

The repair branch was freshly read at
`9628872976da8d43c3a17faa89962f9e32d88979`. A new isolated root checkout was
created only after all 3,346 remote blob paths, SHAs, modes and types matched its
cached content. Its base is local `8734df524702729b94f8c5c385993225b552079b`.
No checkout belonging to the prior invocation was used as this root's writer.
Workspace dependencies resolve inside the new checkout; Node is v24.19.0.

The remote README, work-items, package checkpoints, root checkpoint, register,
original finding texts and frozen scope were reread. A new read of baseline
`c38779c71bb92bc15c3fcb89930348e5417aa475` yields the same exact 47 IDs. All 47
original requirement texts are unchanged. Seven are verified; 40 are incomplete.
The current workflows still restrict push execution to main, not repair branches.

## Reconciled results, not repeated implementation

The reviewed Attempt projection and repair-identity packages are already remote
at `3790b19` and `9628872`. Their independent and root tests remain in the prior
run report; the last combined original root run passed 84/84 with zero skips.
These bounded improvements did not close the broader retention or SDK finding.

The Product ControlService port has a separate local source review (17/17 real
local cases) and durable backup. It remains coupled to Controller and Chart and
is not approved for integration without the genuine native CRD/CEL gate. Local
HTTP/SQL/signature success and a 202 pending intent are not native Acceptance.

## Concrete remaining work

The fresh saved audit identifies two actual open-JSON boundaries, rather than
requiring a migration for every old serializer import:

* Original Review input accepts an en-US digest of valid Unicode-key content but
  rejects the unchanged input under sv-SE. Its downstream bundle validator also
  reserializes stored content. An explicit per-evidence codec is under independent
  review; existing compiler evidence has closed ASCII keys and must not acquire
  a new tag under an old persisted graph identity merely as preventive work.
* Original AdapterRuntime, HTTP adapter and FileEffectJournal repeat the same
  local HTTP action after a real SIGKILL and en-US to sv-SE restart: one call for
  same-locale recovery, two identical calls with different child keys across
  locales. Root also reproduced this on the currently integrated source. The
  producer-version/legacy journal-binding correction is actively implemented and
  separately reviewed. An uncertain old effect must never be given a fresh key.

The original active authors retain their isolated package checkouts; root does
not duplicate their implementation or infer abandonment from a heartbeat.
`run4-active-packages.json` gives immutable remote checkpoints and next actions.
Old source is used only after remote content validation. Raw diagnostic success
means that a defect was reproduced, not that its acceptance test passed.

## Reviewed Review-Evidence package

Author `43d2a3b` and independent review `c100f1b` are now backed remotely at
`3c7d3a33981c7bee504d2920b543ec369829fe9f`. Root integrated the reviewed
source over the exact current docs checkpoint `4627f01`, then reran the entire
original Review npm suite, including its new 5/5 codec/schema/local HTTP cases,
and 9/9 original source/recovery cases. All completed with no skips. Original
compiler bytes remain unchanged; its archived review-enabled graph recovers.
Seven archived source identities also match a freshly fetched untruncated
remote tree. Raw root logs are `run4-root-review-*` in the evidence directory.
The strict schema correction preserves the original accepted path domain; it
does not weaken strict validation. This is no native LLM/Gateway proof.

Adapter work is safely backed at `fc14ecca` but incomplete: original HTTP
SIGKILL/replay/corruption checks are independently exercised. The new reader's
manual request validator is not approved in place of the canonical contract;
author and reviewer are resolving the original URL-resource/key mismatch.

A new safe native alternative has succeeded where the previous context probe
had not tested anything: original kube-apiserver v1.35.0 and etcd start locally
with disposable loopback-only configuration, and accept the original controller
CRD. The complete signed-transition/CEL matrix and separate review are still
pending. No existing cluster, Deployment or workload was used. This narrow
progress does not close the coupled Product/controller/chart package.

Next: complete Adapter canonical binding and the coupled native admission tests;
finish independent reviews and preserve raw failures as well as successes.
Only exact-parent fast-forward publication is allowed. Native infrastructure
blockers are not repeatedly probed without a new safe alternative. No deployment,
CI, production mutation, purchase or third-party message is authorized.
