# Remaining 40: read-only scheduling audit

Fresh remote integration head: `5c20fedaa8c679206eaad75af4bd0744038c557e`.
Frozen baseline: `c38779c71bb92bc15c3fcb89930348e5417aa475`.
Run: `20260909t2146-run3-scope`.

The original register was fetched separately. Exactly 47 entries originally had
status `implementiert`; their sorted IDs equal the current frozen scope. Current
register has seven verified members and 40 incomplete members. All 40 original
`source_finding_text` values still exactly match the baseline. Original decision
notes and current evidence were read; no finding was closed by this audit.

The adjacent JSON contains one entry for each remaining ID: original required
proof, current status, primary scheduling category, exact next action, evidence
and prerequisites. Categories are scheduling priorities, not mutually exclusive
gates: **10 actionable-code, 28 required-runtime, 2 authorityblock**. Code work
may still require native acceptance before integration. Runtime availability
never grants forbidden deployment/CI authority. No tests or unchanged native
prerequisites were rerun.

## Immediate scheduling

1. Continue the already assigned SDK repair-authorization and Attempt-v2 quota
   packages; do not duplicate their work. Fresh remote backup discovery confirms
   Attempt source WIP at `8862643bd0def10004a0f43b743bbfc2d8d19be9`, beyond the
   older proof-only inventory. The WIP report still describes its earlier
   reproduction: backup presence is not a new approved implementation.
2. Registry IFR-08-002 has genuine missing code, not merely unavailable tests.
   The current remote `my-values/infra/registry-local.yaml` still has no volume
   and explicitly admits no reference-aware GC. Its delete-enabled HTTP lab
   registry is not an implemented durable lifecycle. A safe next package needs
   original producer reference registration before push/use, persistent transfer
   across lease/demo/wait/reopen, bounded inventory and GC fences against new
   references/pushes. Persistence and capacity must be explicit operator inputs;
   no guessed PVC size/class or caller keep-list authority. Native lease cycles,
   surviving digest pull and Pod replacement remain the original full proof.
3. Worker-002/-003 have real cross-component ownership work. Fresh remote
   `worker-operation.ts` still freezes shared-process CPU at execution settlement,
   measures shared RSS, and returns literal process count 1. The reviewed final
   Core measurement slice is necessary but does not fix these observations.
   An atomic generic owner plus role-engine execution host is required;
   browser/capability/adapter/evidence/log work must share honest local ownership.
   No disconnected scaffold or process-difference patch closes this cause.
4. Product/controller/chart are saved, locally reviewed but not integrated.
   Port their original Control wrapper to current `ControlService`; the current
   `control.ts` is a thin real pg.Pool listener. Never overwrite it with an old
   backup. Native CRD/CEL installation/cost acceptance remains a real coupled
   integration gate even when the feature is disabled.
5. IFR-25-001 has remaining actual readiness/alarm integration. The Envoy
   candidate is intentionally unapplied until its pinned native gate passes.
   A stderr readiness event is not an acknowledged operator alarm.

## T01-F02: exact gate, not a blanket platform requirement

The original finding requires the actual original two-module graph, complete
source/candidate and cumulative coverage bindings, no Delivery before required
Acceptance/final gates/access handoff, and the specified optional review/test
agent variants with lint still mandatory. D02/D03/D11 explicitly supersede a
universal mandatory agentic architecture review.

The original wording does **not** impose Kubernetes, Chromium or a live model on
every compiler assertion. The current remote compiler already composes source,
module, cumulative and demo stages; graph success still must not be confused
with Ready or Accepted. The chosen unfinished authenticated human product writer
does depend on the saved Product/controller/chart package. Its actual CRD/CEL
installability gate is therefore a genuine prerequisite for that implementation,
not a retrospective expansion of the original compiler requirement. Keep the
separate F-T14 original real Tailnet/app-auth/operator/TTL proofs separate.

Current main reports saying the human writer is missing and saved package reports
saying it is implemented are not a contradiction: the latter code is not yet
integrated. Source and local signature/SQL tests are not full lifecycle acceptance.

## PATH-T02-002: precise external-authority boundary

Fresh remote diagnostic backup:
`3c0f1fb442997065365090ef09fef52b00826424`.

Original Control/SQL/tool rejects late A1 before A2 document mutation. A claimed
A1 keeps the session occupied; A2 cannot be accepted after the stale A1 callback
because the only genuine existing local completion receipt does not prove that
the external gateway has stopped. The diagnostic executes real Core+Node child,
real artifact HTTP upload, finish and disk reopen: it deliberately remains
NeedsNova and is not a pass for A2 acceptance.

The next prerequisite is the actual installed gateway/session terminal
attestation contract and producer evidence. A new authenticated, bound retirement
transition must preserve late callback fencing. Source supersession, missing
callback, timeout, local process exit or a service caller's asserted actor are
not external termination authority. No browser/model call requirement is added
to the original stale-generation test.

## Evidence provenance and limits

Fresh remote reads included all mandatory resume files, current/baseline
registers, frozen scope, both run2 inventories, run2 branch refs, resource
ownership/terminal measurement, infrastructure, Prism, product, registry,
telemetry and trust-readiness reports. The saved Product implementation and
independent review were read at their immutable backup commit, as were the
Prism002 and Attempt boundary reports. Direct current remote source spot checks
covered the registry manifest, Worker operation/attempt adapter, thin Control
entrypoint and Project compiler.

No source, register, status, workflow, remote ref or infrastructure was changed.
No CI, deployment, process impersonation, fake native prerequisite, model call or
third-party message occurred. This report is scheduling evidence only and must
not be added as a successful native test.
