# Run 20260909t233622: reviewed coupled integration and continuing packages

Fresh remote README, work-items, package checkpoints, root checkpoint, register,
original requirements and scope were read at `b06c06dc`. The baseline commit
`c38779c71bb92bc15c3fcb89930348e5417aa475` again yields exactly the same 47 IDs;
every original finding requirement remains unchanged. All 3,773 candidate blobs,
modes and types were verified against a freshly fetched untruncated remote tree
before this invocation's own checkout was created. The preceding root checkout
is read-only. Existing active authors and independent reviewers continue owning
their disjoint packages; no abandoned work is taken over from heartbeat guesses.

The complete coupled Product/controller/chart package and its native evidence
are now published as `4f70d8f13e282e01d014fe82557d37c90d52cb71`, exact parent
`b06c06dce3364c482a1da5637c36bc2b14ba8c72`, exact tree
`a92a55844d829d5cfd9d2ef397c54f937d035a80`. This matches frozen local `3ea43f4`
and includes independent review `6ff6061e08e371c1b92107496447d03dd190c079`,
the exact original 73 package paths, root actual native API/CRD/CEL/signature/
ACK-loss tests, Product 21 registered (17 distinct) and Chart six cases plus
Prism types. Full raw native startup and shutdown logs are explicitly retained.
No source changed after those passes; fresh head and unchanged workflow triggers
were rechecked, and the only repair-branch ref update was fast-forward. The
backup is `fix/resume-47-20260909t233622-coupled-integrated` at that same commit.

Original IFR-18 is now verified; **eight of 47 verified, 39 incomplete**. No full
Product/Tailnet/login/delivery/human acceptance or deployment is claimed.

Three disjoint implementation/review pairs continue:

1. SDK run-frozen transport: the first payload-marker design collides with a
   historically legal model key. Independent real HTTP failure is preserved.
   Correct placement is outside payload, in the original closed invocation and
   effect contracts, with exact identity/receipt matching and dependency ownership.
   Old snapshots retain old producer payload and transport semantics; v3 is selected
   only at genuine new-run creation. Do not silently reinterpret open model data.
2. Nova dispatch projection: genuine original quality-stage/effect/attempt/artifact
   linkage and canonical validation repair the observed cross-Core and malformed-
   request holes. Complete the evidence and race matrix; retain explicit-revision
   eligibility and fail-closed derived-source scope until its original artifact
   authority is proven. A real errored/cancelled test is not a provider success.
3. Envoy trust readiness: actual official binary and native tests now run. Independent
   review exposed an accepted default-SA/SVID mismatch and reversed expired-fixture
   dates. Fix both, retain failing evidence, reconcile the newly integrated charts,
   and rerun native tests before approving source. SPIRE attestation and a delivered
   alarm remain separate, unexecuted requirements.

Current exact package refs and next actions are in `run5-active-packages.json`.
Space remains constrained after the prior ENOSPC event. Reuse immutable dependency
cache files only, with all workspace links resolved into the current checkout;
never install or edit hardlinked dependency content. Reclaim only owned reproducible
data, not another invocation's source/evidence. No CI, deployments, paid resources
or third-party messages. Overall completion is false; no completion notice.
