# Saved continuation of the 39 incomplete findings

Started **2026-09-11 14:20:27 UTC**, hard deadline **16:20:27 UTC**.
Stopped before the deadline after saving executable bounded work. Required
remaining acceptance is blocked by native prerequisites, separate review or
external actions outside the authorized scope. No unchanged blocked gate was
repeated merely to consume the remaining time.

Counts remain **94 verified / 39 partially implemented / 2 in progress / 19 open**.
The original 47-ID wave still has 8 completed and 39 incomplete findings.
The exact per-ID progress and remaining requirement are in
[run-20260911-a51d-2h-state.json](run-20260911-a51d-2h-state.json).
All original finding texts and statuses are preserved.

## Saved source and evidence

- Branch: `fix/resume-39-20260911-a51d-two-hour`.
- Final checkpoint: `d22ee1d298aa63050673e075b363ae4c996008dc`.
- Tested source checkpoint: `c471313251070a0b176219c792b031267548e9f3`.
- Tested full source tree: `d2d1525bc1fbbc367f0d054828008512df7de30a`.
- Earlier checkpoint: `c71134ab4c126a0c4744998a114f80dccdd87774`.
- Integration parent: `49cd5af3443cb3c587ed47967e19e2584355cd7b`.
- [Detailed report and evidence links](https://github.com/datrab/kubeclaw/blob/d22ee1d298aa63050673e075b363ae4c996008dc/docs/review/remediation/implementation/resume-39-20260911-2h.md).

Each checkpoint's changed files were read back byte-for-byte. The candidate tree
matched the local full tree. A second clean checkout ran the complete Delivery
suite with no concurrent source edits: **13 groups passed, zero failed/skipped**.
The final checkpoint adds that raw result and evidence hashes, without changing
tested production source. No production source has been integrated by this run.

## Concrete work

Thirty-four additional original v2/v3 consumer negatives now pass: 28 media and
durable import-CAS/reopen cases plus six ambiguous/missing/non-JSON gate-reference
cases. Genuine completed imports and successful controls prevent missing-test-
setup failures from masquerading as boundary evidence. Original requests,
receipts and journals are retained losslessly. Full fresh tests also include the
original contracts/compiler/locales/body/ref cases, twelve actual SIGKILL
histories and four cancellation scenarios. Upstream envelopes remain explicit
contract vectors, not native provider/model results.

Seven blocking Delivery lint errors were repaired by separating validation and
evidence ownership. Cross-role integration tests were moved out of shared/Buster
packages, preserving all original assertions and npm entrypoints. The unchanged
ownership gate passes; Summary, telemetry, real local HTTP/demo authentication,
pipeline contracts, Prism contracts and original import projection pass.

Prism's large engine method was separated into render/capture/provider owners.
All four original engine lint violations are resolved. **36 existing tests** and
**12 exact original-result/HTML-byte comparisons** pass. Shared Worker Core,
cache identity and browser-close/cancellation obligations are preserved.
Prism and actual Studio TSX typechecks pass. The earlier Studio type-error and
Control-entrypoint lint notes were stale; those source files match integration.

One combined run correctly failed its last control when a package test path was
edited during discovery. The raw failure remains saved. The final clean-source
13/13 run resolves this race without relaxing the registry integrity fence.

## Remaining acceptance

Every one of the 39 is still incomplete against its original requirement. The
per-ID JSON preserves the exact next action; the principal blockers are:

- New tests/refactoring require separate review. Full pinned Knip 6.27.0 is not
  installed or cached. Genuine coupled semantic producer/history acceptance
  remains open; the previously platform-flagged helper was not retried, rephrased
  or routed elsewhere. Semantic source must not be integrated alone.
- The native direct-command runner was executed after the regular sandbox build:
  `write EPIPE`, `open task children: No such file or directory`. Original
  assertions remain intact. Process-tree/cgroup/OOM and adopted-process proof
  require an appropriate host; no guard or permission bypass was attempted.
- Native PostgreSQL cannot initialize under the root-only UID mapping; PGlite
  evidence does not establish native pooling or cross-connection transactions.
- Chromium remains absent after the prior cancelled network approval. No install
  retry occurred. Browser termination/capture/heap/iframe/image/CSP proofs remain
  open, along with parallel/child CPU attribution.
- BuildKit/CRI, cluster rollout, measured capacities, SPIRE attestation, DB-fault,
  real Tailnet/recipient/human acceptance and configured OpenClaw/ACP Writer
  proofs require their actual environments. No deployment, CI, third-party
  message, paid resource or production mutation was performed.
- Broader retention still requires its original ownership and uncertainty
  evidence; no automatic log deletion or derived-source authority was invented.

## Resume

Restore the final candidate in an isolated checkout, read its detailed report
and the exact 39-ID JSON, and review the new changes independently. Preserve
historical v2 behavior, the coupled semantic/Delivery acceptance and the shared
Worker Core with role engines. Only unmet original gates with actual available
prerequisites justify new execution; existing local fixtures cannot substitute
for the missing native/provider/human acceptance. Read a fresh integration head
before any future source integration. A saved source checkout and repeatable
tests establish continuation, not automatic scheduler recovery or full closure.
