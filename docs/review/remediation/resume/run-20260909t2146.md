# Run 20260909t2146: resume from verified remote checkpoints

## Authority and restoration

The integration branch was freshly read at
`5c20fedaa8c679206eaad75af4bd0744038c557e`. Its exact tree is
`3c089550cf31e92d8ee164945ac7356b2887ca38`. The fresh isolated checkout's tree
was compared against this remote tree, including every path, blob, mode and type.
Local commit `d57cd46b21a06390a190c84e5e07d8cbaa4ad6e0` is only the equivalent
local restoration base, not a separately integrated production change.

The remote README, work-items, package checkpoints, root checkpoint, register and
embedded original finding requirements were read again. The 47 original IDs
whose status was `implementiert` at
`c38779c71bb92bc15c3fcb89930348e5417aa475` exactly match `partial-47-scope.json`.
Seven are verified and 40 remain incomplete. Overall completion remains false.

No old checkout is used as a writable continuation. Prior committed content is
only a cache after comparison with remote checkpoints. Uncommitted work in an
older checkout is neither adopted nor treated as abandoned based on a heartbeat.
Workspace dependency links resolve within the new checkout; runtime is Node
v24.19.0. All ten current workflow files were inspected: push triggers are
restricted to `main`, not the repair branches. No workflow was started.

## Bounded packages in progress

* PCR-SDK-001: continue the genuine RepairAuthorization locale reproduction from
  `fix/resume-47-run2-repair-4db5905`, remote
  `a29a9df8cce605f393fa0bec413604a5ae426eeb`. Implement a producer-selected
  completion-event identity version together with live/recovery/admin consumers;
  never infer it from the older snapshot version. Preserve validated historical
  approval identities and legitimate completion-only legacy prefixes. A separate
  reviewer checks genuine old-producer/new-reader and tampering cases.
* PCR-OBS-002: continue the explicit unfinished Attempt-result projection from
  `fix/resume-47-run2-attempt-768fde8`, remote
  `8862643bd0def10004a0f43b743bbfc2d8d19be9`. Retain full completion/evidence
  authority and reconstruct results from the exact original import journal.
  Independent review reproduced event-loop lock contention in this WIP; it is
  not approved for integration. Fix the critical section and verify actual
  consumer, concurrency, corruption and interrupted-write behavior.

Each author and reviewer owns a separate checkout. New source remains incomplete
until independent review and root's integrated tests pass. Neither bounded slice
alone establishes the whole SDK or retention finding.

## Independently checked Attempt integration

The corrected Attempt package was approved separately at reviewer `ab18293`
against author `30e6a4d` (production `6ee3f37`). Root inspected its source,
independent negative cases, preserved lock failure and correction, then combined
it with the current repair-branch source. The exact original Attempt/Admission,
replay, bounded inventory and artifact-checkpoint tests passed **45/45, zero
skips** at local integration `8a242f352bdc989a0aa1fa0cc8bba85314e1b75c`.
Raw output: `docs/review/evidence/run3-root-attempt-final.txt`.

The actual mixed completed/waiting Core and real Store/Reconciler case releases
19,668 bytes and one resident result slot while preserving original result,
completion, evidence and duplicate replay. Explicit WorkerResult fixtures are
store inputs, not a claimed native Buster execution. PCR-OBS-002 remains open.
The next-package audit corrects stale assumptions: telemetry already has a
connected RecordStore retirement path; other consumer records cannot simply be
deleted using that primitive. See `implementation/run3-observability-next-package.md`.

## Independently checked SDK repair identity integration

Reviewer `abcd748c79fa7763967ce2ef0e90438e50927f8f` approved the corrected
repair-identity package after **53/53** original tests. Root inspected the
producer tag, legacy semantic/causal binding, administrative reconstruction,
negative before/after probes and all seven archived source blob identities.
The archive matches a freshly fetched untruncated original remote tree; runtime
probes materialize source and dependencies from the current checkout without
historical Git objects.

Root then reran the exact original repair/administrative/legacy/locale suites,
artifact checkpoint suites and independently reviewed Attempt projection suite
together at `0531c08c70a1c17c743760bf383a45305ca3bf32`: **84/84 passed, zero
skips**. Both full Nova and plugin-runtime typechecks passed. Raw evidence:

* `docs/review/evidence/run3-root-sdk-final.txt`
* `docs/review/evidence/run3-root-integrated-types.txt`
* `docs/review/evidence/run3-root-legacy-archive.json`

Both actual old untagged and new tagged producers preserve the original approval
through en-US to sv-SE recovery and original Core resume, execute the fourth
source attempt and finish the controlled fixture. This is not a gateway/model
or native infrastructure acceptance claim. PCR-SDK-001 stays incomplete while
remaining actual digest consumers are audited; a closed ASCII-key schema is not
automatically a locale defect merely because it imports the old serializer.

## Other saved work and next action

The Prism overlap diagnostic is saved at
`fix/resume-47-run2-prism002-45468a8`, remote
`3c0f1fb442997065365090ef09fef52b00826424`. It establishes a local completion
boundary, not authenticated gateway termination; do not invent an A1 cancellation
or unlock A2 by fabricating a successful tool result.

Product/controller/chart remain coupled and unintegrated pending their genuine
native CRD/CEL gate. Port later Product changes into the current ControlService
composition; do not overwrite it with an old Control entrypoint. No deployment,
CI execution, production change, resource purchase or third-party message is
authorized. Unchanged native prerequisites are not repeatedly retried.

Next: complete the two bounded implementations, retain independent failing and
passing raw tests, publish their checkpoint branches, then integrate only reviewed
net deltas over a freshly read repair-branch parent using fast-forward-only ref
updates. On a changed head, reconcile and rerun affected tests first.
