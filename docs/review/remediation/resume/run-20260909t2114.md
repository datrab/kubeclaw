# Repair continuation checkpoint

Remote starting head: `cbd014b19f6f3498ff709df2e4557357d4f3aac5`.
Local source baseline: `147a9e0` in a new isolated worktree.

The remote README, work-items, package checkpoints, root checkpoint, register
and frozen scope were fetched afresh. The baseline register at
`c38779c71bb92bc15c3fcb89930348e5417aa475` was separately fetched: exactly 47
entries had status `implementiert`, identical to the frozen scope. At entry,
six of those entries are verified and 41 remain incomplete.

All top-level source, test, contract, configuration, lockfile and workflow
Git object IDs in the local baseline match the newly fetched remote tree.
Only the historical documentation subtree differs; publication must therefore
apply this invocation's named delta onto the remote parent, not replace its
whole tree. Current instruction/register files were read remotely.

The fresh worktree resolves `@kubeclaw/nova-core` and `@kubeclaw/plugin-sdk`
inside this worktree. Node is v24.19.0. Original SDK JSON-domain baseline tests
pass. No persistent disk or old-agent liveness is assumed.

## Assigned bounded work

- PCR-SDK-001: select and repair one remaining persisted semantic identity
  domain with actual producer/reader evidence and a separate reviewer.
- PATH-T02-001 / PATH-T02-002: original Control HTTP plus real SQL service
  proofs, preserving authentication, admission and transaction guards. Separate
  reviewer checks original-scope completeness; no browser, model or native
  PostgreSQL claim may be inferred.

Outstanding Product/controller/chart checkpoints remain unintegrated and keep
their documented native acceptance gate. No infrastructure retry loop, CI,
deployment, production mutation or third-party message is authorized.

## First reviewed package

SDK implementation `4baa569` and independent review `27bbf4b` are integrated
locally at `7339edd`. Root inspected the production diff and reran both focused
suites: 8/8 passed, zero skipped. The actual prior defect was checkpoint
reserialization with legacy ordering despite a portable ArtifactStore write.
Encoding now participates in request/response binding, duplicate identity and
journal replay; unsupported encodings reject. The original SIGKILL/CLI recovery
also passed under independent review. Broad PCR-SDK-001 stays incomplete.

Root raw output: `docs/review/evidence/run2-root-checkpoints-final.txt`.
The original SDK-domain baseline also passed. A preventive delivery-manifest
version change was deliberately not shipped: the accepted closed schema did
not substantiate the hypothesized dynamic-key locale defect. See the updated
canonical-consumer inventory rather than repeating that investigation.

Prism HTTP evidence is backed up at
`ff325cc9fec943049a9e715828468dd91971b87a`, but its initial factory structure is
still under revision for newly introduced lint violations. This backup is not
source approval or finding closure. The A1/A2 reconciliation diagnostic also
remains explicitly incomplete. That initial checkpoint did not increase the
verified count.

## Reviewed Prism integration and seventh acceptance

Source `961f606` removes the newly introduced oversized factory by using bounded
service composition and the existing configuration adapter. Independent review
`0c04904` reran 18 original HTTP/SQL/config cases, typecheck and focused lint.
Root inspected the new service, configuration and original HTTP test, then
repeated both actual HTTP cases and three configuration cases on the combined
local head `a134377`: all five pass, no skipped cases. One HTTP case is only the
explicitly incomplete A1/A2 diagnostic, not its acceptance.

PATH-T02-001 now meets its original requirement: stored authenticated consent
and feedback survive actual database/service close and reopen; a fresh session
and new project produce the captured original agent job and prompt containing
the exact persisted snapshot; the original registered tool commits three real
direction records referring to that snapshot. Foreign identity and consent
disable remain checked. Native PostgreSQL pool, Envoy, OpenClaw execution and
design quality are not claimed or substituted for separately required gates.

Root raw files:

- `docs/review/evidence/run2-root-prism-final.txt`
- `docs/review/evidence/run2-root-prism-config.txt`

The register and frozen ledger now contain seven verified findings and 40
incomplete findings. Existing handler lint debt is documented rather than
suppressed. The old Product backup's changes to `server/control.ts` MUST be
ported into the new ControlService structure and re-reviewed; do not overwrite
the thin production entrypoint or bypass the coupled native CRD/CEL gate.

Next active packages are the proven repair-authorization locale mismatch and
the Attempt-v2 redundant-result projection. Their before/proof checkpoints are
saved separately; no success for either is inferred from this checkpoint.
