# Delivery manifest owner — independent preparatory review

Status: classification and proposed boundary are sound; NOT implementation-ready
design approval, source authorization, runtime acceptance or finding closure.
Reviewer run5_storage, isolated run17-delivery-review, 2026-09-10.

## Authority and actual review

Reviewed design `13f0aa8262a20c31aac25a62b63e3aa018155768`,
`docs/review/remediation/design/run11-delivery-manifest-identity-boundary.md`,
against fresh MAIN `ef5399be16c58eb6b8cf0398e2e183c1226d27c9`, tree
`4ef964cfe95019a754c7aabcaef6f78469d47a28`. All4,090 non-tree entries
(mode/type/blob) match the new private checkout. Fresh remote resume README,
work-items, package-checkpoints, root-checkpoint, active inventory, continuation,
register and original PCR-SDK-001 requirements were read. Fresh original
c38779c71bb92bc15c3fcb89930348e5417aa475 status extraction gives exactly47
implementiert IDs, identical to current partial-47-scope.json.

Read the full fc56f1957d042b7d057769c26c5907995c42ef90 Summary before log,
author classification and child/test sources. Inspected current original Summary
builder/stage/schema, compiler/coverage/recovery, remote evidence adapter,
verifiedArtifactJsonText, import-store reader and demo evidence/source linkage.
No implementation or test was run; no claim of independent runtime reproduction.

## Evidence supported, and not supported

fc56 preserves an actual returned delivery-manifest.v2 digest inequality:
English adee836993c1cd9c9ff09d496bdba1dffb285088fb4fd0caa3fe9dee0dc11830
versus Czech 7757bf621fed180598c3cb460d3016910dbc89cacef510924dd447dc0f6dd23e.
Its same-locale control passes and cross-locale assertion fails; zero skips.
The original EffectCoordinator, disk FileEffectJournal and ArtifactStore execute.
Explicit contexts and artifact-contract fixtures supply upstream ownership and
attempt data: this is not a registered PipelineRunner Summary lifecycle or
successful upstream native provider chain.

The child calls buildSummary, not the registered stage's final artifact write.
It saves only input refs, the returned digest and bundle digest temporarily;
the parent deletes that directory. Durable raw does not preserve the complete
unsigned returned manifest, its bytes, a stored delivery manifest ref or an
executed downstream evidence-adapter result. Consequently exact differing
manifest fields and byte-order-only causality remain to be shown, not inferred
as a completed producer/storage/reader proof. The preparatory design accurately
retains these limits.

## Sound proposed separation

An independent finite Summary-owned manifest mode is appropriate. Summary exists
without Review; report outer encoding, Review semantic choice, cache/source
profiles, current locale and compiler freshness cannot select manifest semantics.
A new delivery-manifest.v3 can own a precise portable unsigned-body digest while
the existing explicit Artifact encoding owns stored bytes. These are separate
contracts, not a fallback chain. Both producer and actual downstream digest
reader must change together under independently reviewed graph authority.

Original buildSummary omission, all old compileProject/cumulativeStages arities,
legacy authoring imports and pending semantic compiler work must remain intact.
Fresh creation explicitly opts in; recovery derives mode from the exact expected
Summary node and then compares the WHOLE original graph/pins. Test no-Review,
module-only, final-only and both Review cases with independently varied modes.
A stray node, extra/missing Summary, type swap or unknown selector must not
normalize to a usable graph.

Historical Summary reads and put_json operation/resource/value/ordinal/absence
of encoding remain exact. Historical downstream get_latest_json_bytes arguments
also remain exact. Accepted uncertainty is not a retry license or terminal
receipt; completed replay cannot gain new identity or duplicate calls.

## Required clarification before final implementation design

1. Preserve the current v2 READER domain, not merely its usual producer output.
   Original Summary writes v2 untagged, but evidence-adapter delegates outer
   bytes to verifiedArtifactJsonText, which also admits correctly tagged portable
   bytes under a full matching ref. assertManifest does not prohibit that tag.
   Do not retroactively demand absent ArtifactRef.encoding for all valid v2
   inputs. Require the portable tag for new v3 as proposed, while preserving
   existing v2 acceptance and digest semantics. Same-byte cases do not authorize
   algorithm inference or a silent semantic upgrade.
2. Freeze the actual v3 contract before source work: exact required/optional
   fields, nested module/final bindings (including independently optional Review
   expectations), bounds, unsigned digest body and full-ref rules. Current
   Summary result schema is generic StageResult, not a closed delivery schema.
   Its downstream v2 assertion is deliberately not a complete producer schema;
   new v3 strictness must not silently narrow that legacy domain.
3. Decide the legitimate owning package/API versus bounded mirrored version map
   and parity checks before implementation. Two independently evolving handwritten
   validators or private cross-plugin source imports are not acceptable. Any
   public package change must retain discovery, dependency closure and pin gates.
4. Freeze exact compiler helper options/defaults and Summary config admission.
   The mode is not an extra field accepted from arbitrary authored project data.
   Original canonical config validation plus no-trap owning admission must happen
   before artifact effects. Preserve the independent pending Review semantic
   input expectation instead of conflating or overwriting it.

These are concrete preimplementation decisions, not permission for this reviewer
to implement or execute the missing proof.

## Missing genuine gates and scope limits

A separately authorized original registered Summary proof must preserve actual
Core lifecycle, full constructed unsigned manifest, stored bytes/ref and journal
request/receipt. Compare actual values before attributing all divergence to the
serializer. Contract-vector upstream evidence remains explicitly labeled even
when registered Summary itself executes. Fresh production and cross-locale
reopening are distinct proofs.

Actual downstream evidence-adapter execution must use that stored generated
manifest, preserve full-ref/latest conflict checks and decision uniqueness, then
retain the exact boundary reached. Passing manifest integrity alone is not
passing readVerifiedResult or projectDemoEvidence. The current latter owner
requires passed original auth/build/deployment/exposure nodes and exact typed
source links. Existing evidence-projection tests explicitly use remote envelope
contract vectors; they do not establish a native full chain. Do not substitute
errored/imported results, fake journals, synthetic providers or deployment actions
to turn this gate green. Required genuine unavailable upstream authority stays
open under the user's no-deployment/production/CI/paid-resource restrictions.

After separately approved final design/source, required regression includes old
and new write/read requested/accepted/completed histories, actual SIGKILL with
disk journal/CAS, archived original producer/compiler resolution from current
checkout, unchanged package pins, malformed selector/no-trap JSON, version/tag
mismatch, wrong body/digest/bytes/size/ref/producer, ambiguous gate/coverage and
real import failure. Preserve original Summary/evidence-adapter/demo-candidate
assertions. Any contract-level positive must be distinguished from native
provider evidence. Independent frozen-source review and full fresh-head raw
must precede bounded integration.

## Durable next action and publication guard

Root decides whether the missing allowed producer/reader proof can be authorized,
and obtains the finite contract/default decisions above before production work.
This is a separate owner from the currently authorized Review inner extension.
No tests, code, register or MAIN updates were performed by this review. The
earlier safety-flagged helper action was not retried, rephrased or rerouted; no
future gate listed here authorizes that action. Broader SDK and all47 remain open.

All ten repository workflow blobs match freshly verified MAIN. Actual branch
push triggers target main; remaining triggers are PR/manual/call/schedule.
Publication is this one document on a new fix/resume-47 backup ref with [skip ci],
not a MAIN update, CI run, deployment or third-party communication.
