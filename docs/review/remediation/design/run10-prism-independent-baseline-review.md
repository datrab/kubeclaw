# Independent Prism baseline locale audit and bounded design review

Read-only audit approved as a genuine admitted persisted-consumer counterexample, not full Control/render acceptance. Fresh a43aa256bdce35d7f9c44d5d661e59c4055e562e mandatory continuation/register/original PCR-SDK requirements were read. Frozen audit bcc493caa17a44777376e68288378f8296a232cb exactly matched all3,844 local blobs/modes/types (local9ea17d9042846742279b3390cef00bf82f32756a), then own run10-prism-audit-review was created. No production source changed.

Original audit independently rerun: original validators accept lowercase aa/az assets, genuine PNG bytes and original filesystem CAS produce/reopen artifact:sha256:640854337c4be7df697e49d086d3920c818d5aa6df49e2d8daf3ee06443b86d3. Native en-US original Nova importer accepts; native da-DK rejects identical CAS-digest-verified bytes with PRISM_ARCHIVE_BUNDLE_DIGEST_MISMATCH. Raw run10-prism-independent-locale.txt. Original contract suites2/2 pass; their renderer checks are not native browser capture/Control endpoint publication.

Source chain inspected: original Control stableRecord and checksum-map sorting use localeCompare; asset ID-to-path mapping admits aa/az. /v1/baselines validates exact current approved source, captures actual worker evidence, stores immutable archive/CAS digest and baseline row. Existing-baseline early return reuses recorded digest/artifact. approvedRoundBaseline and original Nova approval/import bind exact recorded digest and source. All member/path/base64/size/project/revision/previews/assets checks must remain. Engine.publish returns a different incomplete small manifest bearing v1 and must remain separate. Buster visual bundle digest is unrelated. Generic outer ArtifactStore serialization and inner approved archive identity are distinct owners. The probe extracts the exact original private helper, not a complete HTTP publisher; it fabricates no renderer success claim.

Recommend controlled paired prism.baseline-archive.v2 / prism.baseline-bundle.v2 and required checksumEncoding=kubeclaw-json.utf16.v1. Digest preimage is the portable serialization of the closed {schema:prism.baseline-checksums.v2,algorithm:sha256,encoding:kubeclaw-json.utf16.v1,files:<complete validated checksum map>} document. Reject unknown/mixed/absent v2 tags and encodings. Preserve exact historical v1 algorithms and old bytes/digests; do not reinterpret a stored v1 archive by new comparison, guess locale or retag old approval.

Concrete source binding: extract owning pure production assembler/helper, called with explicit v2 profile only by actual Control new-publication branch after unchanged source/approval/render/member collection. Existing-row early return precedes this selection and never rebuilds/retags old rows. Nova chooses decoding solely from paired archived tags/encoding. New stored tags carry replay authority; no global SDK/run/locale profile inference. Prefer a separately identified v2 schema so original v1 definitions/identity and separate engine result contracts are not relabeled; owning generated validators/dispatch must admit versions explicitly.

Original PCR-SDK serialization gate does not require unrelated complete native Control DB/browser publication merely to test this extracted actual production serializer. Genuine production assembler plus original validators/CAS/importer, real native en/da/tr/sv, preserved v1 vectors, and complete tamper/path/member/limits/approval/project/revision corruption tests can establish this bounded repair. Inspect exact Control wiring, but label that inspection separately from end-to-end DB/render execution. Real captures and approvals cannot be simulated to claim endpoint completion. Root scope approval is required for implementation; broader frozen47 completion is not established.
