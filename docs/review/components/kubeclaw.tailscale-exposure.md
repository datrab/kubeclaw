# kubeclaw.tailscale-exposure

Review-Status: abgeschlossen. Geprüfter Commit: `85ddfcbfc15e078780ea0434fc167e6f9a9b9488`.

1. **Nutzung.** Busterrolle/Manifest registriert Exposurefixture `kubeclaw.tailscale-exposure@1`, retrySafe=false, kubernetes.exposure. Registry→Runner→isolierter provider.js→Parent-TailscaleExposureCapabilityInvoker→Lease-PATCH→Namespacecontroller→Tailscale-Ingress. Liefert öffentliches typed endpoint; kein HTTPinhaltstest. Verbraucher HTTP/browser sind getrennte Provider.
2. **Vertrag.** Genau ein deployment-Valueinput, optionaler Endpointname/Hostname/Pfad/Readinesszeit. Provider kontrolliert Service-DNS und Port; gültiges HTTP:80 scheitert (001). Invoker prüft Controller-namespace, Prefix, Lease/Spec/Status-Service/Port/Expiry gegen Input, nur freigegebene HTTPSsuffixe. Output ist public-endpoint-fixture mit URL, Host, Namespace, Lease, Zeit und Releaseaction. Schema, Manifest und beide Seiten des Controller-Statuspfades gelesen.
3. **Zustand.** can-i get/patch vor Aktion. Prepare patcht purpose=final-preview/exposure plus zufällige owner-Annotation; Controller erstellt Ingress, schreibt exposurePhase/URL. Release schaltet purpose=gate/provider=off und wartet Off. Deployment-namespace wird nicht gelöscht. Endgültiger lokaler Resultcommit bei Engine, externer Ingress kann vorher bestehen.
4. **Fehler.** Fehlendes/mehrdeutiges Input, falsche URL/Suffix, veränderte Lease/Expiry werfen. Vorbereitung versucht bei Fehler Rollback; eigener maximal15s-Abortcontroller, prüft owner und resourceVersion, AggregateError bewahrt ursprünglichen und Rollbackfehler. Statusready ist nicht an aktuellen Patch gebunden (002). Cleanup ignoriert Capability-`ok`-Feld, Produktionsinvoker wirft aber auf eigenen Fehlern; kein daraus behaupteter Produktionsfalse-success.
5. **Zeit/Parallelität.** #wait verwendet absolute Deadline erst nach ungekürztem #lease-Request und prüft Ready vor Deadline, kann maximumExecutionMs überschreiten; äußeres Workersignal begrenzt Gesamtlauf. Kein Parallelitätslock je Lease. Ownerannotation schützt Rollback vor späterem Prepare, normaler Release besitzt diesen Schutz nicht (002). Retry-safe=false verhindert deklarierte normale Wiederholungen.
6. **Recovery.** Cleanup rekonstruiert deployment aus ursprünglichem Input; Owneruuid lebt nur im lokalen Prepare. Crash nach PATCH lässt Ingress bis Cleanup/TTL bestehen. Abgelaufene/gelöschte Lease wird beim Release nicht als bereits-clean akzeptiert (#verifyLease verlangt Ready und künftige Expiry), daher Cleanup kann nach real erfolgtem TTLdelete Fehler melden. Kein eigener persistierter Exposureabschluss.
7. **Vertrauen.** Keine Credentials im Provider; Parentkubectl erbt kontrollierte Kubeenv/PATH. URLsuffix und HTTPS werden validiert, Kubernetesstatus ist vertrauenswürdige Herkunft, keine externe Signatur. Servicebinding verhindert beliebige URLwahl. Controller prüft Endpoint/Servicebereitschaft; tatsächlich sicherer Netzwerkpfad setzt Operator/CNI/Tailscale voraus.
8. **Ressourcen.** stdout/stderr je1MiB, Requests/Polls begrenzt, keine eigene CPU/RAM-/Prozessmessung (PCR-BUSTER-ENGINE-001). stdin-Errorhandler fehlt auch in dieser Spawnkopie: PCR-KUBERNETES-FIXTURE-002. Tempfiles keine; externe Ingress-/Namespaceaufbewahrung durch ControllerTTL, dessen Tokenrotation ist PCR-BUSTER-NS-001.
9. **Architektur.** Separater Exposureschritt und Deployment-Expiryowner sind verständlich. Benötigt persistenten, generationsgebundenen Exposurestatus statt bloß Ready+aktuell gesetzter Metadatenannotation. Gemeinsame Prozessausführung würde aktuelle Duplikation/Fehler beseitigen.
10. **Tests.** `plugins/tailscale-exposure/tests/live-function.test.ts` vollständig gelesen/ausgeführt: **bestanden**, reine Config/Inputtests mit Port8080. `check-pipeline-tailscale-exposure-implementation.mts` gelesen/ausgeführt: **blockiert** an hartem existsSync('/usr/local/bin/kubectl')-Assert; danach vorgesehene injected-execute-Rollbacktests nicht gelaufen. Keine echte Tailscaleexposure. Logs `../evidence/buster-tailscale-{validation,implementation}-original.txt`. Eigene Originalfunktionsprobe `node docs/review/evidence/buster-tailscale-port80.mjs` **bestanden als Defektreproduktion**: Input mit :80 wird abgewiesen, keine Ersatzimplementierung.
11. **Doku.** README **vorhanden** und zum prinzipiellen Broker-/Cleanupablauf zutreffend. Readinessnachweis **unvollständig** bezüglich aktualisiertem Patch und parallelen Exposures. Port80-Ausschluss steht weder im Configvertrag noch README; Kubernetesfixture kann genau diesen Port liefern. Gegenstellencontroller ausführlich separat geprüft.
12. **Befunde.** 001 dynamisch mit Originalfunktion, 002 statischer vollständiger PATCH/GET/Status-Trace. Cluster-/Neustart-/Updateproof offen, nicht als bestandener Lauf gewertet.

## PCR-TAILSCALE-001 — Standard-HTTPport wird abgewiesen

**Mittel; nachgewiesener Defekt.** `plugins/tailscale-exposure/src/provider.js:48–55` konvertiert URL.port direkt zu Number und verlangt >=1. WHATWG URL normalisiert auch explizites :80 im HTTPinput zu leerem String; Number('')=0. Die Fixture liefert gültig `http://service.namespace.svc.cluster.local:80`, Exposure kann sie nicht übernehmen. **Ursache beheben:** effektiven Port mit Schema-Default80 bestimmen und denselben Wert gegen Servicevertrag validieren. **Regression:** vorhandene Originalfunktionsprobe nach Fix positiv machen; :80 und implizites80, :8080, unerlaubter Host und anderer Service müssen getrennt geprüft bleiben.

## PCR-TAILSCALE-002 — Ready und Cleanup nicht an Exposuregeneration gebunden

**Hoch; nachgewiesener Protokollmangel mit statisch belegtem Trigger.** `tailscale-exposure-runtime.ts:187–218` patcht neue owner-Annotation, wartet aber nur `status.exposurePhase==='Ready'`; Controllerstatus enthält keine beobachtete Owner/Generation. Ist vorherige Exposure bereits Ready, liefert nächster GET aktuelle Metadatenowner mit altem Ready/URL; Namespace/Expiry/Suffix stimmen, falscher Pfad/Hostname kann als neues erfolgreiches Result akzeptiert werden. `#release:229–239` prüft darüber hinaus keinen Owner und schaltet eine inzwischen durch anderen Attempt erneuerte Exposure ab. **Ursache beheben:** Controller quittiert Exposureowner oder Specdigest im Status, Prepare verlangt genau diese Generation; Release führt atomaren Owner-/Resourceversionvergleich aus und behandelt bereits abgelöste eigene Exposure als nicht mehr zuständig. **Regression:** echter APIserver oder explizite Protokollpräfixprobe nach PATCH vor Controllerstatus-Update sowie zwei parallele Prepare/Cleanup-Attempts; alter Status darf nicht new-ready sein, alter Cleanup darf neuen Ingress nicht deaktivieren.
