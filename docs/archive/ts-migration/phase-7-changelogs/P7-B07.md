# P7-B07 — Nova Tooling, Summary, and Adapter Registry

## Scope Migrated

- Migrated the static adapter registry and summary-session/project-summary service adapters to TypeScript:
  - `skills/nova/pipeline/services/adapter-registry.ts`
  - `skills/nova/pipeline/services/summary-session-cleanup.ts`
  - `skills/nova/pipeline/services/summary/project-summary.ts`
- Migrated lint-report entrypoint and internals to TypeScript:
  - `skills/nova/pipeline/tools/lint-report.ts`
  - `skills/nova/pipeline/tools/lint-report/*.ts`
- Migrated project-summary formatter/generator owners and the Nova Redis adapter tool to TypeScript:
  - `skills/nova/pipeline/tools/project-summary-formatters.ts`
  - `skills/nova/pipeline/tools/project-summary.ts`
  - `skills/nova/pipeline/tools/redis.ts`
- Left `.js` files only as thin runtime/direct-CLI facades that forward to the TypeScript owners and preserve accepted external `/app/skills/pipeline/tools/*.ts` paths.
- Updated TypeScript callers and verification source inspections to target the TypeScript owners directly.

## Fallback Ledger Decisions

### KEEP_TYPED_POLICY Retained

- `tools/lint-report/constants.ts`: retained bounded operator defaults for tier and tool timeout.
- `tools/lint-report/container-yaml-tools.ts`: retained applicable-file clean results, parse-failure findings, and scoped changed-file behavior.
- `tools/lint-report/execution.ts` / `report.ts` / `tool-registry.ts`: retained nonthrowing external-tool results, timeout reporting, missing-binary skips, per-tool exception mapping, explicit/platform ESLint/Semgrep config policy, and scoped changed-file filtering.
- `tools/lint-report/output.ts`: retained noncritical trace-log append failure policy.
- `tools/project-summary.ts`: retained Git-tracked-empty filesystem walk for untracked/generated project trees, incomplete gate visibility as `PENDING`, and non-authoritative Discord notification behavior.
- `tools/redis.ts`: retained direct/operator Redis adapter defaults, `sendTask(...)` as an external adapter alias, presentation-only payload identity aliases, nonblocking Discord notification, strong completion identity fail-closed behavior, archive stream naming, and symlink-aware direct-entry guard.
- `services/summary/project-summary.ts`: retained optional generator outputs and noncritical generator/artifact/Discord failures as failed generator results instead of pipeline crashes.
- `services/adapter-registry.ts`: retained only the documented fail-closed allowlist and verification dependency-injection seams.

### STRICTIFY_TS_SLICE Strictified

- `P3-091`: replaced loose regex/comment-stripping export extraction with a token scanner that ignores comments/strings and parses ESM/CJS export forms without regex-based source rewrites.
- `tools/lint-report/discovery.ts`: directory read failures now accumulate typed lint-report discovery diagnostics instead of silently returning empty results.
- `tools/project-summary.ts` Git command failures now produce typed `diagnostics` entries with `source:'git'`, command, status, and reason while preserving partial summary generation.
- `tools/project-summary.ts` JSON reads now distinguish `missing`, `malformed`, and `unavailable` diagnostics instead of collapsing all optional artifact failures to `null`.

### DELETE_LEGACY Deleted 100%

- `P3-092`: deleted title-text regex scope grouping. Delivered-scope groups now come from structured module metadata (`scope_group`, `scopeGroup`, `scope.group`, `category`, or metadata equivalents); unknown/missing metadata is explicitly `other`.
- `P3-093`: deleted prompt filename census. Agent invocation counts now read structured `agent.spawned` telemetry from `.swarm/logs/pipeline/pipeline.jsonl` and run-scoped `pipeline.jsonl` files with event de-duplication.
- No deleted project-summary prompt-directory walk, title-regex grouping branch, or regex export extraction branch remains in the migrated TypeScript owners.

## Native Node TypeScript Compatibility

- All migrated TypeScript uses erasable syntax only with explicit relative `.ts` / `.js` imports.
- Preserved accepted `.js` runtime/CLI facades without using transform-only TypeScript syntax.
- Native import evidence covered all 15 migrated TypeScript files.

## Verification

- Unsupported syntax scan:
  - `node:module.stripTypeScriptTypes(..., { mode: 'strip' })`
  - Result: `{"ok":true,"checked":15}`
- Native Node import check:
  - Imported all 15 migrated TypeScript owners directly.
  - Result: all imports passed.
- `scripts/typecheck-ts-migration.sh`
- Focused checks:
  - `node tests/verification/contracts/check-critical-dynamic-imports.mjs` -> `{"ok":true,"checked":43}`
  - `node tests/verification/contracts/check-generator-result-surface.mjs` -> `{"ok":true,"checked":48}`
  - `node tests/verification/contracts/check-strict-cli-args-surface.mjs` -> `{"ok":true,"checked":36}`
  - `node tests/verification/contracts/check-implementation-map-sync-surface.mjs` -> `{"ok":true,"checked":10,"issue":"RV-18"}`
  - `node tests/verification/behavior/verify.mjs --areas foundations,runtime-surface,docs-surface,governance` -> `passed: 63, failed: 0`
- Full fast verification:
  - `tests/verification/run-fast-verification.sh` passed.
- Whitespace:
  - `git diff --check` passed.
