# P8-B03 — Module Runner Forge and Buster Phase Policy

## Scope

- Audited the module runner Forge/Buster phase owners against the Phase 8 fallback ledger.
- Locked existing strict behavior with focused module-runner contract coverage.
- Updated migration docs for Forge-only durable publication and Buster typed failure classification.

## Fallback-Ledger Decisions

- `P3-060` is resolved as `DELETE_LEGACY`: Buster dispatch requires a typed nonempty `test_suites` list and the queued notification renders only that validated list.
- `P3-061` remains `KEEP_TYPED_POLICY`: polling Git failures fail closed with terminal `EXIT_ERROR` and operator-visible Git evidence.
- `P3-062` is resolved as `DELETE_LEGACY`: Forge-only PASS requires durable Git commit/push evidence and does not use soft-fail publication.
- Module-runner no-work/progress routing remains tied to typed worker/completion result reasons; no transcript-text classifier was introduced in this batch.
- Terminal Buster failure routing requires explicit typed `failure_class` evidence and does not classify from Redis verdict presence or source regexes.

## Changes

- Updated `tests/verification/contracts/check-module-runner-slice-surface.mjs`:
  - added source guards for nonempty Buster suite validation and no `none` queued-suite fallback;
  - added source/runtime guards that Forge-only Git publication failure returns terminal `EXIT_ERROR`;
  - added guards for fail-closed Buster polling Git failures;
  - added guards against terminal Buster verdict/source heuristic classification.
- Updated Phase 8 status and architecture/authority docs for the hardened module phase policy.

## Deleted Files

- None.

## Verification

Passed:

```bash
scripts/typecheck-ts-migration.sh
node tests/verification/contracts/check-module-runner-slice-surface.mjs
node tests/verification/contracts/check-session-authority-slice-surface.mjs
node tests/verification/contracts/check-redis-completion-service-surface.mjs
node tests/verification/behavior/verify.mjs --areas foundations,runtime-surface,module-failures,polling
tests/verification/run-fast-verification.sh
git diff --check
```

Notes:

- Module-runner slice contract passed with `checked=110`.
- Focused behavior verification for `foundations,runtime-surface,module-failures,polling` passed with `118 passed, 0 failed`.
- Fast verification passed, including the deterministic contract suite and fast behavior areas.
