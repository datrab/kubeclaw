# P9-B03 Changelog — Buster Support-Service Implementation Owners

Commit: this commit (`refactor: migrate buster support services to typescript`).

## Summary

- Migrated six Buster support-service implementation owners from JavaScript to native Node-strippable TypeScript:
  - `skills/buster/pipeline/services/capabilities.ts`
  - `skills/buster/pipeline/services/gateway-health.ts`
  - `skills/buster/pipeline/services/orphan-recovery.ts`
  - `skills/buster/pipeline/services/pipeline-helpers.ts`
  - `skills/buster/pipeline/services/runtime.ts`
  - `skills/buster/pipeline/services/session-monitor.ts`
- Deleted obsolete declaration shims:
  - `capabilities.d.ts`
  - `gateway-health.d.ts`
  - `orphan-recovery.d.ts`
  - `pipeline-helpers.d.ts`
  - `runtime.d.ts`
  - `session-monitor.d.ts`
- Updated Buster source, verification, and active migration docs to import/read the `.ts` owners directly.
- Updated the Phase 9 unpaired JavaScript contract from 31 to 25 remaining unpaired `.js` files.

## Node Type Stripping

- The migrated files remain erasable TypeScript:
  - no enums;
  - no runtime namespaces;
  - no parameter properties;
  - no decorators;
  - no import-equals;
  - explicit relative `.ts` / `.js` imports.
- Native Node import check passed for all six migrated `.ts` files.
- The migrated files keep behavior unchanged; legacy untyped object bodies are marked `@ts-nocheck` while exported call boundaries needed by typed callers are annotated where required. This keeps the batch focused on extension ownership and runtime compatibility without adding fallback behavior.

## DELETE_LEGACY

- No P9-B03-owned fallback-ledger row was marked `DELETE_LEGACY`.
- Deleted stale `.js` implementation files completely; no paired same-path `.js` facade was left behind for these six behavior owners.
- Deleted stale `.d.ts` files that existed only to type JavaScript owners.

## STRICTIFY_TS_SLICE

- No P9-B03-owned row was marked `STRICTIFY_TS_SLICE`.
- Existing external-adapter alias rows remain at the service boundaries named by the ledger:
  - capability aliases normalize at capability service boundaries;
  - Discord webhook env alias remains in the runtime helper boundary;
  - rate-limit max-pause aliases remain in `pipeline-helpers.ts`;
  - monitor dispatch/session identity fallback remains in `session-monitor.ts`.

## KEEP_TYPED_POLICY

- Retained durable denied-capability operator alert fan-out and sanitized stderr fallback.
- Retained gateway readiness retries and periodic health threshold shutdown policy.
- Retained orphan active-session file evidence as diagnostic-only, never lifecycle authority.
- Retained output-file fail-closed guarantee and monitor/result gap conversion to explicit task outcomes.
- Retained operator embed fallback rendering and Discord field truncation.
- Retained cleanup policy: task-scoped cleanup only with identity, startup/shutdown sweeps only for those stages, disabled otherwise.
- Retained gateway degraded/restored de-dupe, hard-timeout termination outcome policy, and explicit nonterminal rate-limit exhaustion.

## Verification

Passed before commit:

```bash
node --input-type=module -e "await import('./skills/buster/pipeline/services/capabilities.ts'); await import('./skills/buster/pipeline/services/gateway-health.ts'); await import('./skills/buster/pipeline/services/orphan-recovery.ts'); await import('./skills/buster/pipeline/services/pipeline-helpers.ts'); await import('./skills/buster/pipeline/services/runtime.ts'); await import('./skills/buster/pipeline/services/session-monitor.ts'); console.log('native imports ok')"
node tests/verification/contracts/check-phase9-unpaired-js-surface.mjs
scripts/typecheck-ts-migration.sh
node tests/verification/contracts/check-buster-pipeline-slice-surface.mjs
node tests/verification/contracts/check-rate-limit-slice-surface.mjs
node tests/verification/contracts/check-gateway-operation-boundary-surface.mjs
node tests/verification/contracts/check-session-authority-slice-surface.mjs
node tests/verification/runtime/check-buster-startup-smoke.mjs
node tests/verification/behavior/verify.mjs --areas foundations,runtime-surface,buster-runtime-normalization
tests/verification/run-fast-verification.sh
git diff --check
```
