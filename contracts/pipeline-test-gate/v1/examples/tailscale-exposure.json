{
  "fixtures": {
    "public-exposure": {
      "uses": "kubeclaw.tailscale-exposure@1",
      "needs": ["deployment"],
      "config": { "path": "/", "readinessTimeoutSeconds": 120 },
      "inputs": { "deployment": { "from": "deployment", "output": "deployment" } }
    }
  },
  "tests": {
    "public-health": {
      "uses": "kubeclaw.http@1",
      "needs": ["public-exposure"],
      "config": { "path": "/", "expectedStatuses": [200], "requestTimeoutMs": 10000 },
      "inputs": { "endpoint": { "from": "public-exposure", "output": "exposure" } }
    }
  }
}
