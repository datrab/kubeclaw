import type { SuiteVerdict } from '../services/verdict-schema.js';
import type { TestCredentialSpec } from './k8s-credentials.js';
import type { K8sCommandEnv } from './k8s-command-env.js';
import type { AnyRecord, Check, K8sContext, SuiteLog } from './k8s-base.js';
export interface K8sPlan {
    context: K8sContext;
    payload: AnyRecord;
    moduleId: string | undefined;
    startTime: number;
    log: SuiteLog;
    commandEnv: K8sCommandEnv;
    repoRoot: string;
    sourceImage: string | null;
    dockerfile: string | null;
    buildContext: string | null;
    imageName: string;
    serviceName: string;
    manifestPaths: string[];
    port: number;
    healthPath: string;
    readyTimeout: number;
    namespacePrefix: string;
    secretsToCopy: string[];
    leaseTimeout: number;
    ttlSeconds: number;
    cleanupPolicy: 'keep' | 'delete';
    purpose: 'final-preview' | 'pretest';
    previewProvider: string;
    previewPath: string;
    previewCredentialsRef: string | null;
    previewCredentialSecretName: string | null;
    previewCredentialsKeys: string[];
    previewRevealCredentials: boolean;
    previewExpectedText: string | null;
    testCredentialSpecs: TestCredentialSpec[];
    previewUrlTimeout: number;
    buildTimeoutMs: number;
    pushTimeoutMs: number;
    deployTimeoutMs: number;
    healthRetries: number;
    healthBaseDelayMs: number;
    runId: string;
    namespace: string;
    leaseName: string;
    previewCredentialCommand: string | null;
    previewHostname: string;
    previewExposure: AnyRecord;
    registryTag: string;
    namespaceLease: AnyRecord;
    checks: Check[];
    criticalFailed: boolean;
}
type PlanResult = {
    ok: true;
    plan: K8sPlan;
} | {
    ok: false;
    verdict: SuiteVerdict;
};
export declare function prepareK8sPlan(context: K8sContext, startTime: number, log: SuiteLog): PlanResult;
export {};
