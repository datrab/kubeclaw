# Buster plugin packages

This directory owns Buster-specific v2 package candidates. Packages remain
non-authoritative until their migration unit has real implementation parity and
immutable gate evidence. Forwarding wrappers do not count as migration.

The Buster bundle overlays this directory with `skills/common/plugins` at
`/app/skills/plugins`.
