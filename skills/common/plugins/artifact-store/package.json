{
  "name": "@kubeclaw/plugin-artifact-store",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node tests/package-boundary.test.mjs && node --experimental-strip-types tests/live-function.test.ts"
  }
}
