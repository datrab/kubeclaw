# Plugin Foundation Boundary

This package contains role-neutral plugin registry, configuration, isolation,
artifact, and package functions. It has no pipeline policy. It has no worker
or specialist execution meaning.
