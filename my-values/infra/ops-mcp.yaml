apiVersion: v1
kind: ServiceAccount
metadata:
  name: kubeclaw-ops-mcp
  namespace: kubeclaw
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: kubeclaw-ops-mcp-readonly
  namespace: kubeclaw
rules:
  - apiGroups: [""]
    resources: ["pods", "pods/log", "services", "events"]
    verbs: ["get", "list"]
  - apiGroups: ["apps"]
    resources: ["deployments", "statefulsets", "daemonsets", "replicasets"]
    verbs: ["get", "list"]
  - apiGroups: ["batch"]
    resources: ["jobs", "cronjobs"]
    verbs: ["get", "list"]
  - apiGroups: ["networking.k8s.io"]
    resources: ["ingresses"]
    verbs: ["get", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: kubeclaw-ops-mcp-readonly
  namespace: kubeclaw
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: kubeclaw-ops-mcp-readonly
subjects:
  - kind: ServiceAccount
    name: kubeclaw-ops-mcp
    namespace: kubeclaw
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: kubeclaw-ops-mcp-applications
  namespace: argocd
rules:
  - apiGroups: ["argoproj.io"]
    resources: ["applications"]
    verbs: ["get", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: kubeclaw-ops-mcp-applications
  namespace: argocd
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: kubeclaw-ops-mcp-applications
subjects:
  - kind: ServiceAccount
    name: kubeclaw-ops-mcp
    namespace: kubeclaw
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ops-mcp
  namespace: kubeclaw
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: ops-mcp
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ops-mcp
    spec:
      serviceAccountName: kubeclaw-ops-mcp
      imagePullSecrets:
        - name: ghcr-secret
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: ops-mcp
          image: ghcr.io/datrab/kubeclaw-ops-mcp:REQUIRES_DIGEST
          imagePullPolicy: IfNotPresent
          env:
            - name: OPS_DEFAULT_NAMESPACE
              value: kubeclaw
            - name: OPS_ALLOWED_NAMESPACES
              value: kubeclaw
            - name: OPS_MCP_BEARER_TOKEN_FILE
              value: /var/run/ops-mcp-auth/token
            - name: ARGOCD_NAMESPACE
              value: argocd
            - name: HUBBLE_SERVER
              value: hubble-relay.cilium.svc.cluster.local:4245
          volumeMounts:
            - name: backend-auth
              mountPath: /var/run/ops-mcp-auth
              readOnly: true
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 2
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 20
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            capabilities:
              drop: ["ALL"]
      volumes:
        - name: backend-auth
          secret:
            secretName: ops-mcp-auth
            defaultMode: 0444
            items:
              - key: token
                path: token
---
apiVersion: v1
kind: Service
metadata:
  name: ops-mcp
  namespace: kubeclaw
spec:
  selector:
    app.kubernetes.io/name: ops-mcp
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP
---
# Optional private operator endpoint for testing from devices in the tailnet.
# ChatGPT itself should use Secure MCP Tunnel rather than this private URL.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ops-mcp
  namespace: kubeclaw
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: ops-mcp
      port:
        number: 8080
  tls:
    - hosts:
        - ops-mcp
