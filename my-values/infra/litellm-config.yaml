model_list:
  # Embeddings only: OpenClaw memory.search remote endpoint.
  - model_name: gemini-embedding-001
    litellm_params:
      model: vertex_ai/gemini-embedding-001
      vertex_project: "ornate-loader-487422-u0"
      vertex_location: "global"

general_settings:
  master_key: os.environ/LITELLM_MASTER_KEY

litellm_settings:
  set_verbose: false
  drop_params: true
